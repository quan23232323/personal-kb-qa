# PersonalKB-QA 面试题与答案解析

> 基于本项目（RAG 知识库问答系统）整理的高频面试题，按 5 大类组织。答案均结合项目实际代码，力求"讲清原理 + 落到代码"。建议先自己作答，再看解析。

---

## 目录

- [一、RAG 与 AI 基础（10 题）](#一rag-与-ai-基础)
- [二、FastAPI / Python 后端（12 题）](#二fastapi--python-后端)
- [三、数据库与存储（8 题）](#三数据库与存储)
- [四、前端 React / TypeScript（8 题）](#四前端-react--typescript)
- [五、安全、工程化与场景题（10 题）](#五安全工程化与场景题)
- [六、项目优化迭代专题（6 题）](#六项目优化迭代专题本次新增)

---

## 一、RAG 与 AI 基础

### Q1. 什么是 RAG？为什么需要它？
**答案**：RAG（Retrieval-Augmented Generation，检索增强生成）是把**检索**和**生成**结合的架构：先从知识库检索与问题相关的内容，再把检索结果作为上下文交给大模型生成回答。

需要它的三个原因：
1. **私有知识**：大模型训练时没有你的内部文档，无法回答"我们公司的请假制度"这类问题；
2. **幻觉问题**：没有依据时模型会编造，RAG 提供真实资料能显著降低幻觉；
3. **知识时效**：模型有训练截止日期，RAG 能接入最新资料。

**本项目落地**：`chat_service.py` 的 `chat()`/`_chat_stream_impl()` 就是 RAG 编排——`_retrieve()` 检索 → `_build_prompt()` 拼上下文 → `llm_service` 生成。

---

### Q2. 什么是 Embedding？为什么文本能参与数学运算？
**答案**：Embedding 是把文本映射成一个固定维度的**浮点数向量**，让"语义相近的文本在向量空间里距离也近"。

计算机无法直接理解语义，但可以计算向量间的相似度（如余弦相似度）。"苹果手机怎么样"和"iPhone 好不好用"向量接近，和"今天天气不错"向量远离。这样"语义检索"就变成了"向量最近邻搜索"。

**本项目**：`embedding_service.py` 用 `sentence-transformers`（`text2vec-base-chinese`，中文效果好）或 OpenAI 的 embeddings 接口，`embed()` 返回 `list[list[float]]`。

---

### Q3. 余弦相似度和余弦距离是什么关系？本项目里为什么要 `1 - distance`？
**答案**：
- 余弦相似度衡量两个向量**方向**的接近程度，范围 `[-1, 1]`，越接近 1 越相似；
- 余弦距离 = `1 - 余弦相似度`。

**关键坑**：ChromaDB 建 Collection 时显式指定 `hnsw:space = "cosine"`，此时它 `query` 返回的 `distances` 字段是**余弦距离**（距离越小越相似），不是相似度。所以代码要转换：

```python
# retrieval_service.py
"similarity": round(1.0 - distances[i], 6)
```

然后 `chat_service._retrieve` 里用 `similarity >= threshold` 过滤。

> 如果建 Collection 时用默认 L2 度量，`distances` 的语义完全变了，再用 `1 - distance` 和 0.5 阈值就是错的——这是 DESIGN.md v1.1 修正的核心 bug。

---

### Q4. 为什么要把文档切块（chunking）？chunk_size 和 chunk_overlap 分别是什么？
**答案**：切块原因：
1. Embedding 模型有最大输入长度限制；
2. 检索希望返回"精准小片段"而非整篇文档，让 Prompt 聚焦。

**chunk_size**：每块目标字符数（本项目 500）；
**chunk_overlap**：相邻块重叠字符数（本项目 50）。重叠是为了避免把完整语义从中间切断、导致检索时上下文丢失。

**本项目**用 `RecursiveCharacterTextSplitter`，并显式指定**中文分隔符**：

```python
separators=["\n\n", "\n", "。", "！", "？", "；", "，", " ", ""]
```

> 追问：为什么不用默认分隔符？—— 默认是英文导向（按空格、换行切），中文没有空格，会导致切分质量差；加上中文标点后按语义边界切，效果更好。

---

### Q5. top_k 和 similarity_threshold 分别控制什么？空检索怎么办？
**答案**：
- **top_k**：检索返回最相似的前 K 个片段（本项目 5）；
- **similarity_threshold**：相似度阈值，低于它直接丢弃（本项目配 0.4），防止无关内容污染 Prompt。

**空检索兜底**：过滤后为 0 条时，直接返回 `"知识库中暂无相关信息，无法回答该问题。"`，**不调用 LLM**。好处：
1. 省 LLM 调用成本；
2. 避免模型没依据时硬编（幻觉）。

代码在 `chat_service.py` 的 `NO_INFO_ANSWER` 分支。

---

### Q6. 为什么要给 LLM 写"如果参考资料中没有相关信息，请如实告知"？
**答案**：这是**抑制幻觉的提示词技巧**。模型倾向于"尽力回答"，即使参考资料无关也可能编造。明确指令约束它"没有就如实说"，能显著降低编造率。

本项目 `SYSTEM_PROMPT` 里就写了这句话，并强制"根据以下参考资料回答"，把模型从"自由发挥"约束到"基于资料"。

---

### Q7. 流式输出（SSE）和非流式输出的本质区别是什么？为什么 axios 不行？
**答案**：
- **非流式**：等服务端完整生成完，一次性返回全文，用户要等很久；
- **流式**：服务端边生成边推送 token，前端逐字渲染（打字机效果），体验好。

**为什么 axios 不行**：axios 底层会等响应体完整接收才触发回调，拿不到"逐条"增量数据。SSE 需要按 `data:` 行增量读取，必须用 `fetch + ReadableStream` 或 `@microsoft/fetch-event-source`。

**本项目**：后端用 `sse-starlette` 的 `EventSourceResponse` 包裹生成器；前端 `api/stream.ts` 用 `fetchEventSource`，按 `msg.type` 分发到 `onToken/onSources/onDone/onError`。

---

### Q8. SSE 和 WebSocket 有什么区别？聊天流式输出为什么选 SSE？
**答案**：

| 维度 | SSE | WebSocket |
|------|-----|-----------|
| 方向 | 单向（服务器→客户端） | 双向 |
| 协议 | 基于普通 HTTP，自动重连 | 独立协议，需升级握手 |
| 复杂度 | 轻量 | 较重 |
| 适用 | 单向推送（如流式输出） | 双向实时（如聊天室、游戏） |

聊天流式输出只需"服务器持续推给客户端"，不需要客户端频繁回发，SSE 更简单、更贴 HTTP 生态，够用且好维护。

---

### Q9. 本项目如何用同一套代码同时支持 DeepSeek、OpenAI 和 Ollama？
**答案**：关键在于它们都提供了 **OpenAI 兼容接口**，所以统一用 `openai` SDK 的 `OpenAI` 客户端，只是 `base_url/api_key/model` 不同。

- **DeepSeek（默认）**：`base_url=https://api.deepseek.com/v1`、`model=deepseek-chat`；
- **OpenAI**：`base_url=https://api.openai.com/v1`；
- **Ollama（本地）**：Ollama 也暴露了 OpenAI 兼容的 `/v1` 端点：
  ```python
  base = "http://localhost:11434".rstrip("/")
  base_url = base + "/v1"   # Ollama 的 OpenAI 兼容端点
  api_key = "ollama"        # Ollama 不需要真 key，随便填
  ```

这体现**适配器/统一接口思想**：三种模式只是参数不同，调用逻辑完全复用。换 Kimi/智谱/通义等，只需改 `base_url` 和 `model`。

---

### Q10. 向量检索有什么局限？本项目预留了什么改进？
**答案**：局限——纯向量检索靠语义，对**精确术语、编号、代码、专有名词**这类"关键词匹配"场景效果差（比如搜"合同编号 ABC-123"，语义向量可能不敏感）。

**改进方向**（DESIGN.md §14.2 已预留）：
1. **混合检索**：向量检索 + BM25 关键词检索融合；
2. **重排（Rerank）**：对 Top-K 结果用重排模型精排；
3. 分页、文档增量更新等。

---

## 二、FastAPI / Python 后端

### Q11. 为什么这个项目的 FastAPI 端点全部用同步 `def` 而不是 `async def`？
**答案**：这是本项目最核心的并发约定。

- `async def` 跑在事件循环里，如果内部调用阻塞代码（SQLite、ChromaDB、torch embedding、openai HTTP 请求），会**阻塞整个事件循环**，导致所有请求串行卡死；
- 同步 `def` 端点，FastAPI 会**自动放进线程池**执行，阻塞调用不会卡住事件循环。

本项目的 ChromaDB、SQLite、torch、openai 全是阻塞调用，所以统一用同步 `def` 最安全、最简单。需要并行阻塞调用时才用 `await asyncio.to_thread(...)`。

> 延伸：`async def` 适合 I/O 密集且用异步库（如 `httpx.AsyncClient`、异步数据库驱动）的场景，才能真正发挥事件循环价值。

---

### Q12. 中间件有什么作用？本项目为什么要手写 ASGI 中间件而不是用 BaseHTTPMiddleware？
**答案**：中间件在请求进路由前/响应返回前插入处理逻辑。

本项目的 `ApiKeyMiddleware` 之所以**手写纯 ASGI 中间件**（实现 `__call__(scope, receive, send)`），是因为 Starlette 的 `BaseHTTPMiddleware` 会**缓冲响应体**，这会破坏 SSE 流式响应（SSE 需要边生成边发送，不能被缓冲）。代码注释里明确写了这一点。

鉴权逻辑：`security.api_key` 为空则禁用；启用后校验 `/api/*` 的 `X-API-Key` 头，`OPTIONS`（CORS 预检）跳过。

---

### Q13. FastAPI 里如何处理异常，让所有接口返回统一的 `{code, data, message}`？
**答案**：注册异常处理器 + 统一的成功/失败封装函数。

`main.py` 注册了三个 `@app.exception_handler`：
- `HTTPException` → 用 `fail(detail, status_code)`；
- `RequestValidationError`（Pydantic 校验失败）→ 422；
- `Exception`（兜底）→ 500。

`models/common.py` 提供：

```python
def ok(data=None):  return {"code": 0, "data": data, "message": "success"}
def fail(message, code=-1): return {"code": code, "data": None, "message": message}
```

成功路径由各 router 手动 `return ok(...)`。这样前端拦截器能统一解包。

---

### Q14. Pydantic 在这个项目里起什么作用？`models/` 目录是 ORM 吗？
**答案**：Pydantic 用类型注解定义数据结构并自动校验/序列化。本项目的 `models/` **不是 ORM**，而是 Pydantic schema（请求/响应模型）。

例如 `KnowledgeBaseCreate.name` 用 `Field(..., min_length=1, max_length=200)`，不合规的请求会被 FastAPI 自动拦截返回 422，无需手写校验。

> 项目刻意**不用 SQLAlchemy ORM**：表只有 4 张、结构简单，标准库 `sqlite3` + 手写 SQL 更轻、零依赖、且与 DDL 一致。

---

### Q15. BackgroundTasks 是真正的异步吗？它适合什么场景、不适合什么场景？
**答案**：`BackgroundTasks` 会在**响应发送之后**执行任务，但它**不是完全独立的异步 worker**——它仍占用当前请求的线程/进程资源。

- **适合**：轻量、快速的后台工作（本项目文档解析+向量化，个人场景文档不大）；
- **不适合**：长时间、高并发、需要失败重试/监控的任务——那种场景应该用 Celery、RQ 等独立任务队列 + 消息中间件。

---

### Q16. 为什么要把 chromadb、torch 等 import 放在函数内部（惰性导入）？
**答案**：这些是"又大又慢"的重依赖。放在文件顶部，意味着"只做知识库 CRUD"也必须先安装并加载它们。

惰性导入后：
- `/health` 和 CRUD 接口在没装重依赖时也能跑；
- 只有问答/文档处理真正调用时才 import，失败时返回清晰错误；
- 启动速度更快、内存占用更低。

**代码位置**：`retrieval_service.get_client()` 里 `import chromadb`、`embedding_service._get_local_model()` 里 `from sentence_transformers import SentenceTransformer`。

---

### Q17. 单例缓存（模块级变量）在本项目里怎么用？有什么好处？
**答案**：模块级变量只在模块首次导入时初始化一次，之后复用。

本项目：
- `embedding_service._local_model`：只加载一次 embedding 模型（模型加载很慢，不能每次请求都加载）；
- `retrieval_service._client`：只创建一个 `PersistentClient`。

好处：避免重复初始化开销。注意模块级单例在多线程下要保证线程安全——这里模型 encode 和 Chroma client 本身是可复用、相对安全的。

---

### Q18. 生成器（generator）和 `yield` 在流式输出里怎么起作用？
**答案**：生成器函数用 `yield` 逐个产出值，不一次性执行完。

`llm_service.chat_stream` 是生成器，逐 chunk `yield` token；`chat_service.chat_stream` 也是生成器，`yield` 一个个 SSE 事件 dict。`EventSourceResponse` 会逐个消费这些事件、逐个发给客户端，实现边生成边推送。

```python
for token in llm_service.chat_stream(prompt):
    collected.append(token)
    yield {"event": "token", "data": json.dumps({"type": "token", "content": token})}
```

---

### Q19. 流式问答里，为什么 assistant 消息要由后端持久化而不是前端？
**答案**：为了**数据完整性**。前端只负责逐步渲染 token，如果由前端存库：
1. 用户中途刷新/关闭页面会丢数据；
2. 前端拼接的 token 可能和后端实际生成的不一致；
3. 历史记录、会话切换、多端访问都无法保证一致。

所以约定：**后端在 `done` 前自行累积全部 token、把 assistant 消息（含 sources）写入 SQLite**，并在 `done` 事件返回 `message_id`/`session_id`。代码在 `chat_service._chat_stream_impl` 里 `collected` 累积、`_save_message` 持久化。

---

### Q20. 为什么统一响应要包裹成 `{code, data, message}`？直接返回裸对象不行吗？
**答案**：统一包裹的好处：
1. **协议一致**：前端拦截器只需写一次解包逻辑（`code !== 0` 抛错，否则返回 `data`）；
2. **错误信息结构化**：成功和失败用同一结构，便于统一提示；
3. **可扩展**：以后可以加 `trace_id`、分页信息等字段而不破坏现有客户端。

DESIGN.md v1.1 明确修正了"接口表裸对象 vs 统一包裹"的矛盾，所有非流式接口统一包裹。

---

### Q21. 为什么上传文档接口读文件用 `file.file.read()` 一次性读入？有什么隐患？
**答案**：`UploadFile.file.read()` 会把整个文件读进内存。个人知识库文件小，实现简单、代码清晰。

**隐患**：大文件会占用大量内存，且 50MB 限制是在读完内存之后才判断的（顺序是"先 read 再判大小"），恶意/超大文件可能先撑爆内存。

**改进**：流式边读边写（`shutil.copyfileobj`）、先看 `Content-Length` 预判、或分块读取边写边计数，超限即中止。

---

## 三、数据库与存储

### Q23. 为什么这个项目不用 ORM，而用标准库 sqlite3？
**答案**：
1. 数据模型简单（只有 4 张表），ORM 带来的便利有限；
2. 标准库零依赖、更轻量；
3. 手写 SQL 与 DDL 完全一致，透明可控；
4. 项目是本地单用户/小规模，不需要 ORM 的连接池、迁移等复杂能力。

代价是：字段映射、JSON 序列化等要手写（如 `sources` 手动 `json.dumps/loads`）。

---

### Q24. SQLite 的外键约束默认开启吗？为什么本项目要 `PRAGMA foreign_keys = ON`？
**答案**：**默认不开启**。SQLite 出于历史兼容原因，外键约束默认是关闭的，必须每个连接手动 `PRAGMA foreign_keys = ON`。

本项目依赖 `ON DELETE CASCADE`（删知识库自动级联删文档/会话/消息），所以 `database.py` 的 `get_connection` 里显式开启了它，否则级联删除不生效，会出现"孤儿数据"。

---

### Q25. 删除一个知识库要按什么顺序做什么事？为什么？
**答案**：`kb_service.delete_kb` 的顺序：
1. 删 SQLite 记录（documents/sessions/messages 靠外键 `ON DELETE CASCADE` 自动删）；
2. 删上传文件目录（`shutil.rmtree`）；
3. 删 ChromaDB Collection（`delete_collection`）。

要点：
- 先删 DB 记录让级联生效；
- ChromaDB 是独立存储，**不会**随 SQLite 自动删，必须显式 `delete_collection`，否则留下"僵尸向量"。
- 文件系统也是独立的，必须手动 `rmtree`。

---

### Q26. 为什么 `document_count` 不存进数据库，而要动态 COUNT？
**答案**：避免数据不一致。如果把它存成字段，每次文档增删都要同步更新，漏更新就会和真实数量对不上。

本项目在查询时用子查询动态计算：

```sql
SELECT kb.*, (SELECT COUNT(*) FROM documents d WHERE d.kb_id = kb.id) AS document_count
FROM knowledge_bases kb
```

这样永远和 `documents` 表实际行数一致，没有"同步"负担。

---

### Q27. SQLite 的 `DEFAULT CURRENT_TIMESTAMP` 会在 UPDATE 时自动更新吗？怎么解决？
**答案**：**不会**。`DEFAULT CURRENT_TIMESTAMP` 只在 INSERT 时生效，UPDATE 不会自动刷新。

解决方式（本项目选 service 层显式更新）：`_save_message` 里每次插消息都顺带：

```python
conn.execute("UPDATE chat_sessions SET updated_at = ? WHERE id = ?", (now, session_id))
```

另一种方式是建触发器（DESIGN.md 里也给了示例），但 service 层显式更新更直观。

---

### Q28. `sources` 是 list，但 SQLite 没有 list 类型，怎么存？
**答案**：序列化成 JSON 字符串存，读出来再反序列化。

- 写：`json.dumps(sources or [], ensure_ascii=False)`；
- 读：`json.loads(d.get("sources") or "[]")`。

`ensure_ascii=False` 保证中文不被转成 `\uXXXX`，可读性好。

---

### Q29. 事务（transaction）是什么？本项目怎么保证事务？
**答案**：事务保证一组 SQL 要么全部成功、要么全部回滚（原子性）。

本项目用 `@contextmanager` 的 `db()`：

```python
@contextmanager
def db():
    conn = get_connection()
    try:
        yield conn
        conn.commit()      # 正常则提交
    except Exception:
        conn.rollback()    # 异常则回滚
        raise
    finally:
        conn.close()
```

`with db() as conn:` 自动完成"建连接 → 提交/回滚 → 关闭"，避免忘记 commit 或 close。

---

### Q30. 为什么给 `kb_id`、`session_id` 建索引？
**答案**：加速按外键的查询。`documents.kb_id`、`chat_sessions.kb_id`、`chat_messages.session_id` 是高频查询条件（列表、历史消息），建索引用空间换时间，避免每次全表扫描。

个人场景数据量小影响不大，但这是正确的好习惯，数据量大时收益明显。

---

## 四、前端 React / TypeScript

### Q31. React 的 useState、useEffect、useRef 分别解决什么问题？
**答案**：
- **useState**：组件内可变状态，更新触发重渲染；
- **useEffect**：副作用（数据请求、定时器、事件订阅），可返回清理函数；
- **useRef**：跨渲染保存可变值，**修改不触发重渲染**，常用于保存 DOM 引用或"最新值"。

本项目例子：
- `const [kbs, setKbs] = useState(...)`；
- `useEffect(() => { load(); }, [id])` 页面/参数变化时加载；
- `useRef` 在流式输出里存累积文本。

---

### Q32. 流式输出里，为什么 state 和 ref 两个都要用？只用 state 会怎样？
**答案**：因为 `onToken` 是**异步回调**，闭包捕获的 state 是"注册那一刻的旧值"。如果只写：

```tsx
setStreamingText(streamingText + t)
```

连续快速回调时，每次都用旧 `streamingText` 累加，会**互相覆盖、丢字符**。

正确做法（本项目）：

```tsx
streamingTextRef.current += t;              // ref 始终是最新值
setStreamingText(streamingTextRef.current); // 用 ref 的值触发渲染
```

**分工**：ref 负责"可靠地保存最新累积值"，state 负责"触发 UI 重新渲染"。

---

### Q33. axios 拦截器在这个项目里做了什么？为什么业务代码能直接拿到 data？
**答案**：响应拦截器统一处理 `{code, data, message}`：

```tsx
client.interceptors.response.use((response) => {
  const body = response.data;
  if (body && 'code' in body) {
    if (body.code !== 0) return Promise.reject(new Error(body.message));
    return body.data;   // 解包，直接返回 data
  }
  return body;
}, ...);
```

这样 `listKnowledgeBases()` 拿到的直接是 `KnowledgeBase[]`，业务代码不用每次手动 `resp.data.data` + 判 code；错误拦截器统一把 `error.response.data.message` 转成 Error 抛出，页面统一 `message.error`。

---

### Q34. 上传文件时为什么不能手动设置 Content-Type？
**答案**：`multipart/form-data` 需要随机生成的 **boundary**（分隔符）来区分文件边界。如果手动写死 `Content-Type`，boundary 会缺失或错误，文件传不完整、后端解析失败。

正确做法：用 `FormData` 直接交给 axios，让 axios 自动生成带正确 boundary 的 Content-Type。

```tsx
const fd = new FormData();
fd.append('file', file);
http.post(url, fd);   // 不设置 Content-Type
```

---

### Q35. 什么是乐观渲染？本项目在哪里用了？
**答案**：乐观渲染（Optimistic UI）指用户操作后，不等待后端确认，先在界面上乐观地显示预期结果，后端失败再回滚/提示。

本项目 `ChatPage.handleSend` 里，发送后立即把用户消息加进列表：

```tsx
const userMsg = { id: `local-${Date.now()}`, role: 'user', content: text, ... };
setMessages(prev => [...prev, userMsg]);
```

好处是发送瞬间就有反馈。缺点是要处理失败回滚（本项目失败时靠 `onError` 提示，未做消息回滚，属可改进点）。

---

### Q36. 文档处理状态是怎么从前端感知到的？为什么用轮询？
**答案**：上传后接口立刻返回 `status='processing'` 的 Document，真正的处理在后台。前端 `KnowledgeBaseDetail` 检测到有 `processing` 的文档时，用 `setInterval` 每 2 秒重新拉取列表，直到没有 processing 为止：

```tsx
const hasProcessing = docs.some(d => d.status === 'processing');
useEffect(() => {
  if (!hasProcessing) return;
  const t = setInterval(load, 2000);
  return () => clearInterval(t);
}, [hasProcessing]);
```

**为什么轮询**：实现简单、无需额外通道。缺点是 2 秒延迟、不实时；更优方案是 SSE/WebSocket 推送状态变化。

---

### Q37. React Router 的动态路由和查询参数分别怎么用？本项目用了哪些？
**答案**：
- **动态路由参数**：`/knowledge-bases/:id`，用 `useParams()` 取 `{ id }`；
- **查询参数**：`?session=xxx`，用 `useSearchParams()` 读写；
- **跳转**：`useNavigate()`；
- **重定向**：`<Navigate to="..." replace />`；
- **嵌套路由 + Outlet**：Layout 作外壳，子页面渲染在 `<Outlet />`。

本项目 `ChatPage` 用 `searchParams.get('session')` 初始化当前会话，用 `setSearchParams({ session: id })` 保持 URL 与会话同步（可刷新/分享）。

---

### Q38. TypeScript 的 interface 和泛型在本项目里怎么用？有什么好处？
**答案**：`types/index.ts` 用 interface 定义数据模型（`KnowledgeBase`、`ChatMessage` 等），与后端 Pydantic 模型一一对应。

**泛型**在 `http.get<T>`：

```tsx
export function listKnowledgeBases(): Promise<KnowledgeBase[]> {
  return http.get('/knowledge-bases');
}
```

好处：编译期类型检查、IDE 自动补全、重构安全，前后端字段错位能在编译期暴露。

---

## 五、安全、工程化与场景题

### Q39. 文件上传有哪些安全风险？本项目分别怎么防？
**答案**：
1. **恶意文件类型**：不只看扩展名，用**魔数校验**文件头（PDF=`%PDF`、DOCX=`PK\x03\x04`），`file_utils.detect_type`；
2. **路径穿越**：文件名可能含 `../../`，用 `sanitize_filename` 取 `basename` + 过滤非法字符 + `{uuid}_` 前缀重命名，落盘路径被严格控制；
3. **超大文件**：`max_file_size_mb`（默认 50）限制，超限返回 413；
4. **扩展名白名单**：只允许 `pdf/txt/md/docx`。

---

### Q40. 什么是路径穿越（Path Traversal）？本项目怎么防？
**答案**：攻击者上传文件名形如 `../../etc/cron.d/evil`，如果直接 `save_dir / filename` 拼接，可能写到任意目录甚至覆盖系统文件。

本项目防御（`file_utils.sanitize_filename`）：
1. `Path(filename).name` 只取文件名部分，剥离目录；
2. 正则过滤，只保留中文/字母/数字/下划线/连字符/点；
3. 落盘时再加 `{uuid}_` 前缀，进一步消除碰撞和路径含义。

---

### Q41. SQL 注入是什么？参数化查询如何防？
**答案**：SQL 注入是把恶意 SQL 片段拼进查询字符串执行。例如 `f"WHERE id = '{kb_id}'"`，传 `' OR '1'='1` 就能绕过条件。

参数化查询用 `?` 占位符，把数据和 SQL 代码分离：

```python
conn.execute("SELECT * FROM knowledge_bases WHERE id = ?", (kb_id,))
```

数据库驱动会安全地处理参数（当作字面值，而非 SQL 代码），从根本上杜绝注入。本项目所有 SQL 都用参数化查询。

---

### Q42. API Key 鉴权是怎么实现的？为什么 `OPTIONS` 请求要跳过鉴权？
**答案**：`ApiKeyMiddleware` 校验 `/api/*` 请求的 `X-API-Key` 头，与 `security.api_key` 比对，不匹配返回 401；`api_key` 为空则整个鉴权禁用。

**OPTIONS 跳过**：`OPTIONS` 是浏览器的 CORS **预检请求**，本身不带业务头（如 `X-API-Key`），如果拦截它会导致跨域预检失败，正常请求也发不出去。所以必须放行。

---

### Q43. 密钥（API Key）为什么要走环境变量而不是写进配置文件？
**答案**：
1. **防止泄露**：配置文件通常会被提交到 git，写死密钥等于公开；
2. **环境隔离**：开发/生产用不同密钥，改环境变量即可，不用改代码；
3. **合规**：很多安全规范要求密钥不进版本库。

本项目两层机制：
1. `config.py` 的 `_expand_env` 把 `${OPENAI_API_KEY}` 替换成环境变量值，找不到就保留原文（容错）；
2. **`_load_dotenv()`（新增）**：启动时自动从 `backend/.env` 读取 `KEY=value` 到环境变量，且**不覆盖已存在的变量**——这样密钥写进 gitignore 的 `.env`，免去每次手动 export，又兼顾"手动 export 的变量优先级更高"。

---

### Q44. 为什么后端默认绑定 127.0.0.1 而不是 0.0.0.0？
**答案**：安全考虑。`127.0.0.1` 只允许本机访问，局域网/公网无法连入，避免把个人知识库暴露出去。

需要局域网共享时，才改 `0.0.0.0`，并且**必须**同时开启 API Key 鉴权（否则任何能访问该端口的人都能读你的知识库、调用 LLM）。

---

### Q45. 这个项目的分层架构是什么？这样分层有什么好处？
**答案**：`Router → Service → Model`（+ utils/middlewares 支撑）。

- **Router**：参数校验、HTTP 状态码、调用 Service；
- **Service**：业务逻辑（CRUD、检索编排、Prompt 组装）；
- **Model**：Pydantic 数据模型。

好处：
1. 职责单一，可读性高；
2. Service 可独立单元测试；
3. 可替换（换向量库只改 `retrieval_service`，换 LLM 只改 `llm_service`），对外接口不变。

---

### Q46. 如果要给这个项目加单元测试，你会测哪些、怎么测？
**答案**：
1. **工具函数**：`file_utils` 的 `sanitize_filename`（路径穿越）、`detect_type`（魔数/扩展名匹配）；
2. **service 层**：`kb_service` 的 CRUD、`document_service` 的分块（中文分隔符效果）、`chat_service` 的空检索兜底；
3. **检索验证**：用固定中文语料建临时 Chroma 集合，验证"召回 + 阈值过滤 + 空检索兜底"；
4. **接口测试**：用 FastAPI `TestClient` 测各接口的 200/400/404/413/500 路径；
5. **统一响应**：验证成功/失败都返回 `{code, data, message}`。

关键难点：embedding/LLM 是重依赖且慢，测试应**mock 掉**（替换成假的 embed 函数/固定向量），保证测试快、可重复。

---

### Q47. 如果文档处理失败，系统是怎么反馈给用户的？状态机是什么？
**答案**：文档有明确状态机：`processing → completed / failed`。

- 上传/重处理时置 `processing`；
- `process_document` 全程 try/except：成功置 `completed` + `chunk_count`，失败置 `failed` + `error_message`；
- 前端列表用 `Tag` 显示状态（处理中/已完成/失败），失败时把 `error_message` 显示出来；
- `reprocess` 接口在文档处于 `processing` 时返回 400"文档正在处理中"，防止并发重复处理。

---

### Q48. 重处理（reprocess）一个文档，为什么要先删旧向量？
**答案**：如果不删，重新解析分块后会把**新向量追加**到旧的之上，同一份文档出现**重复向量**，检索时可能重复召回同一内容、甚至新旧内容混杂。

本项目 `start_reprocess` 先：

```python
retrieval_service.delete_document_vectors(doc["kb_id"], doc_id)  # 先删旧向量
# 再置 processing，重新走流水线
```

`delete_document_vectors` 底层是 `collection.delete(where={"document_id": doc_id})`，按元数据精确删。

---

### Q49. 如果检索结果相关性很差，你会从哪些参数/环节去排查和调优？
**答案**：系统性地排查：
1. **分块**：`chunk_size`/`chunk_overlap` 是否合适，分隔符是否覆盖中文标点；
2. **Embedding 质量**：模型是否适合中文（`text2vec-base-chinese` 是否已归一化），是否与 cosine 度量匹配；
3. **度量与阈值**：确认 Collection 是 cosine 空间，`similarity = 1 - distance` 转换是否正确，`similarity_threshold` 是否过高/过低；
4. **top_k**：是否过小漏掉相关内容；
5. **文档解析质量**：PDF/DOCX 提取是否干净（有无乱码、表格丢失）；
6. **Prompt**：参考资料是否清晰编号、指令是否明确；
7. **数据本身**：文档是否真的包含答案（知识库内容不足时 RAG 也无法无中生有）。

建议做法：准备 10~20 条中文问题，人工核对 Top-K 召回是否相关，据此调参（DESIGN.md §14.1 也这么建议）。

---

### Q50. 如果要给这个项目加"多用户"和"数据量大"支持，你会改哪些地方？
**答案**：
- **多用户**：
  1. 增加 `users` 表 + 登录鉴权（JWT），知识库加 `owner_id`；
  2. 所有查询加"当前用户"过滤，防止越权；
  3. 用更成熟的鉴权方案（如 FastAPI 依赖注入 + OAuth2），替换简单的 X-API-Key。
- **数据量大**：
  1. 列表接口加分页（`page/page_size`，返回 `{items, total}`）；
  2. SQLite 换成服务型数据库（PostgreSQL/MySQL）或至少开连接池；
  3. Chroma 嵌入式换成独立服务/分布式向量库，支持海量向量；
  4. 文档处理从 BackgroundTasks 换成真正的任务队列（Celery + Redis）；
  5. 加混合检索 + 重排提升精度；
  6. 前端轮询换 SSE/WebSocket 推送。

---

## 六、项目优化迭代专题（本次新增）

### Q51. 项目是如何从 Ollama 无缝切换到 DeepSeek 的？体现了什么设计思想？
**答案**：因为 DeepSeek 提供了 **OpenAI 兼容接口**，`llm_service.py` 原本就统一用 `openai` SDK 调用，所以切换只改了 `config.yaml` 三处：

```yaml
llm:
  mode: openai                          # 从 ollama 改成 openai
  openai:
    base_url: https://api.deepseek.com/v1  # 换成 DeepSeek
    model: deepseek-chat                   # 换成 deepseek 模型
```

业务代码一行没动。这体现了**面向接口/适配器设计**：把"用哪个 LLM"做成可配置项，底层差异（base_url/model/key）收敛到配置，调用逻辑不变，实现供应商可插拔。

### Q52. 为什么要支持 `.env` 文件加载密钥？`_load_dotenv` 如何实现"不覆盖已存在变量"？
**答案**：之前密钥只能手动 `export`，每次开新终端都要重复设置，容易忘。支持 `.env` 后，密钥写进 gitignore 的 `.env` 文件，启动时自动加载。

实现要点（`config.py` 的 `_load_dotenv`）：
1. 逐行读 `backend/.env`，跳过空行、`#` 注释、无 `=` 的行；
2. `key.partition("=")` 拆出键值，值 `strip().strip('"').strip("'")` 去掉引号；
3. **`if key and key not in os.environ`** 才写入——即"已存在的环境变量优先，.env 不覆盖"；
4. 在 `load_config()` 之前调用，保证 `${...}` 展开时变量已就位。

这个"不覆盖"细节很重要：它让"手动 export 的变量"优先级高于 .env，便于临时覆盖。

### Q53. LLM 的 key 校验为什么要前置？代码怎么判断 key 没设置？
**答案**：如果不前置校验，用户没配 key 时会一路走到 LLM 调用，最终收到一个晦涩的 401/认证错误，排障成本高。前置校验把错误**提前到最清晰的时机**暴露。

判断逻辑（`llm_service._llm_params`）：

```python
if not api_key or "${" in api_key:
    raise ValueError("OPENAI_API_KEY 未设置：请 export OPENAI_API_KEY=...")
```

- `not api_key`：key 为空；
- `"${" in api_key`：`${OPENAI_API_KEY}` 没被展开（环境变量未设置），还是原始占位符——这比单纯判空更聪明，能精准识别"占位符残留"。

### Q54. 为什么把 `similarity_threshold` 从 0.5 调到 0.4？调参依据是什么？
**答案**：因为 `text2vec-base-chinese` 的余弦相似度分数**整体偏低**，0.5 阈值会把不少真正相关但分数在 0.4~0.5 之间的片段误杀，导致频繁触发"暂无相关信息"兜底。

调到 0.4 后召回更稳（宁可多召回一点，也不漏掉关键片段）。这体现了一个原则：**阈值不是拍脑袋定的，要结合实际模型的分数分布来标定**。正确做法是用 `verify_retrieval.py` 或评测集跑一批问题，观察相关片段的相似度分布，据此设阈值。

### Q55. `verify_retrieval.py` 的作用是什么？为什么要在接入 LLM 前先独立验证检索？
**答案**：它是一个**不依赖 LLM key 的检索链路验证脚本**，用法 `python verify_retrieval.py <kb_id> <query>`，走完"embedding → Chroma 检索"完整链路，打印命中的 chunk 和相似度。

为什么要独立验证：RAG 的**检索和生成是解耦的两段**。如果一上来就联调 LLM，检索有问题时会被 LLM 的回答掩盖，很难定位"到底是检索错了还是 LLM 答错了"。先把检索单独跑通、确认召回合理，再接入 LLM，能大幅降低排障成本——这是"分而治之"的调试思路。

### Q56. 在国内部署这个 RAG 项目，网络相关有哪些坑？怎么解决？
**答案**：两个主要卡点：
1. **pip 安装重依赖慢**：torch/chromadb 等体积大，默认 PyPI 源在国内慢甚至超时。解决：`pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple`（清华镜像）。
2. **HuggingFace 模型下载失败**：首次加载 `text2vec-base-chinese`（~400MB）要连 HuggingFace，国内直连常因 SSL 证书校验失败而重试超时，文档会卡在 `processing`。解决：启动前设置 `HF_ENDPOINT=https://hf-mirror.com`。

关键点：`HF_ENDPOINT` 必须在**启动后端之前**设置（模型是首次使用时才下载）。

---

## 附：面试高频"一句话"速记卡

| 问题 | 一句话答案 |
|------|-----------|
| RAG 是什么 | 先检索知识库相关内容，再交给 LLM 生成，降低幻觉、接入私有知识 |
| 为什么切块 | 模型输入长度限制 + 检索需要精准小片段 |
| cosine 距离 vs 相似度 | 相似度 = 1 − 距离，Chroma 返回的是距离 |
| 为什么全用同步 def | 阻塞调用会被 FastAPI 自动丢线程池，避免卡事件循环 |
| 为什么不用 ORM | 表少且简单，标准库 sqlite3 更轻、零依赖 |
| 为什么手动开外键 | SQLite 默认关闭外键约束，不开 CASCADE 不生效 |
| document_count 为什么动态算 | 避免和真实数量不一致 |
| 为什么手写 ASGI 中间件 | BaseHTTPMiddleware 缓冲响应体会破坏 SSE |
| axios 为什么不能做 SSE | 它等完整响应，拿不到增量数据 |
| 为什么后端持久化 assistant 消息 | 保证历史记录完整、多端一致 |
| 魔数是干嘛的 | 校验文件真实类型，不只看扩展名 |
| 参数化查询干嘛的 | 防 SQL 注入 |
| 怎么换 LLM 供应商 | 都走 OpenAI 兼容接口，改 base_url/model 即可 |
| .env 加载密钥 | 启动自动读 .env，不覆盖已有环境变量 |
| 为什么 key 要前置校验 | 把配置错误提前暴露，而非等 LLM 401 |
| 阈值为什么 0.4 | text2vec 分数偏低，0.5 漏召回，0.4 更稳 |
| verify_retrieval 干嘛的 | 不依赖 LLM key，单独验证检索链路 |
