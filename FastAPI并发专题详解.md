# FastAPI 并发专题详解

> 本项目最核心的一条工程约定写在 DESIGN.md §10.0：
>
> > **所有路由端点统一使用同步 `def`（而非 `async def`）**。FastAPI 会将同步端点自动放进线程池执行，从而 ChromaDB、SQLite、openai、torch embedding 等阻塞调用不会卡住事件循环。禁止在 `async def` 端点里直接调用同步阻塞代码。
>
> 这句话看似简单，背后牵涉了"并发、并行、GIL、事件循环、线程池、阻塞"一整条知识链。本文把它们从头讲透，并落回本项目代码。

---

## 目录

1. [先厘清三个概念：并发、并行、阻塞](#1-先厘清三个概念并发并行阻塞)
2. [Python 的 GIL 是什么、影响什么](#2-python-的-gil-是什么影响什么)
3. [事件循环与协程：async/await 的本质](#3-事件循环与协程asyncawait-的本质)
4. [FastAPI 的两条并发路径](#4-fastapi-的两条并发路径)
5. [为什么阻塞调用会"卡死" async def](#5-为什么阻塞调用会卡死-async-def)
6. [本项目为什么统一用同步 def](#6-本项目为什么统一用同步-def)
7. [深挖：本项目的"阻塞调用"到底阻塞在哪](#7-深挖本项目的阻塞调用到底阻塞在哪)
8. [SQLite 并发与锁（本项目最容易忽略的点）](#8-sqlite-并发与锁本项目最容易忽略的点)
9. [模块级单例的线程安全问题](#9-模块级单例的线程安全问题)
10. [流式 SSE 与并发的交互](#10-流式-sse-与并发的交互)
11. [中间件与并发的坑](#11-中间件与并发的坑)
12. [多请求同时到达，实际发生了什么](#12-多请求同时到达实际发生了什么)
13. [调优：workers、线程池、WAL](#13-调优workers线程池wal)
14. [什么时候该用 async def + 异步库](#14-什么时候该用-async-def--异步库)
15. [本项目的并发隐患清单](#15-本项目的并发隐患清单)
16. [并发专题面试追问集](#16-并发专题面试追问集)

---

## 1. 先厘清三个概念：并发、并行、阻塞

很多混淆都源于这三个词分不清。

- **阻塞（Blocking）**：一段代码执行时，当前线程**被挂起等待**，什么都做不了。例如 `socket.recv()` 等网络返回、`sqlite3` 等磁盘写入、`torch` 等 CPU 计算、`time.sleep(1)`。

- **并发（Concurrency）**：一段时间内**多个任务在推进**，看起来"同时"在做。它不要求真的同时执行——一个人"边煮面边回消息"就是并发（靠来回切换）。

- **并行（Parallelism）**：多个任务**真的在同一时刻**执行，需要多核 CPU。三个人分别干三件事才是并行。

一句话：**并发是"切换得快"，并行是"真的同时"**。

Web 服务器追求的是**并发**：同时处理很多请求，而不是每个请求都用满一个 CPU 核。

---

## 2. Python 的 GIL 是什么、影响什么

**GIL（Global Interpreter Lock，全局解释器锁）**：CPython（最常用的 Python 实现）里的一个互斥锁，保证**同一时刻只有一个线程在执行 Python 字节码**。

**直接影响**：

- 多线程跑**纯 Python 计算**：无法真正并行（GIL 限制），甚至因为切换开销更慢；
- 多线程跑 **I/O 阻塞**（网络、磁盘、`time.sleep`）：**没问题**，因为阻塞时线程会**释放 GIL**，让别的线程跑；
- 多线程跑**调用 C 扩展的耗时操作**（如 numpy/torch 底层、部分数据库驱动）：很多 C 扩展在内部会**释放 GIL**，所以也能获得一定并行。

**结论**：GIL 让"多线程并行 CPU 计算"这条路在 CPython 里基本走不通，但**对 I/O 密集型并发是够用的**——而 Web 服务恰恰大多是 I/O 密集型。

> 补充：Python 3.13 提供了实验性的 free-threaded（无 GIL）版本，但生态还在跟进，主流生产环境仍默认有 GIL。

---

## 3. 事件循环与协程：async/await 的本质

### 3.1 单线程也能"并发"

事件循环（Event Loop）是**单线程**的，它维护一个任务队列，不断"取一个任务 → 执行到它主动让出（await）→ 取下一个任务"。因为切换发生在"等待"的地方，且切换极快，看起来就像多个任务同时推进。

### 3.2 协程与 await

- 用 `async def` 定义的函数是**协程**；
- 函数里 `await` 一个"可等待对象"时，协程会**挂起**，把控制权交还事件循环，让循环去跑别的任务；
- 当被 await 的操作完成，协程会被**唤醒**，从挂起点继续。

关键点：**`await` 后面的操作必须是非阻塞的异步操作**（如 `httpx.AsyncClient.get()`、异步数据库驱动）。如果 `await` 一个阻塞调用，那"挂起"就形同虚设——因为阻塞调用会**占住整个线程**，事件循环无法切换到别的任务。

### 3.3 一张图理解事件循环

```
事件循环（单线程）
  │
  ├─ 任务A：async def handler ── await 网络请求 ──▶ 挂起，让出
  │
  ├─ 任务B：async def handler ── await 网络请求 ──▶ 挂起，让出
  │
  ├─ 任务C：async def handler ── await 网络请求 ──▶ 挂起，让出
  │
  └─ 当某个网络请求完成 → 唤醒对应任务继续执行
```

所有任务共享**同一个线程**，靠"挂起/恢复"实现并发。一旦某个任务里出现**不挂起的阻塞调用**，整个循环就卡死，其他任务全部停摆。

---

## 4. FastAPI 的两条并发路径

FastAPI（底层是 Starlette）根据端点函数是 `async def` 还是普通 `def`，走**完全不同的两条路**：

### 4.1 路径一：`async def` 端点 → 跑在事件循环里

```python
@app.get("/a")
async def a():
    data = await some_async_io()   # 正确：await 异步操作
    return data
```

端点直接在事件循环里执行，`await` 异步 I/O 时让出循环，实现高并发。

**前提**：所有耗时操作都必须是**异步的**（用异步库）。

### 4.2 路径二：普通 `def` 端点 → 跑在线程池里

```python
@app.get("/b")
def b():
    data = blocking_io()   # 阻塞调用
    return data
```

Starlette 检测到是普通 `def`，会用 `run_in_threadpool` 把它**丢进线程池**执行。请求处理逻辑在一个**工作线程**里跑，不占用事件循环线程。

**效果**：即使端点里全是阻塞调用（SQLite、requests、torch），事件循环也**不会被卡住**，仍能继续接收和调度新请求。

### 4.3 线程池的真相（重要）

- 这个线程池来自 `anyio`，**默认容量约 40 个线程**（上限可配）；
- 所有同步端点**共享**这一个线程池；
- 当并发请求数超过线程池容量时，多余的请求会**排队等待**空闲线程；
- 每个请求占用一个线程，直到处理完成才释放。

> 面试话术：能说出"同步端点跑在 anyio 的线程池里，默认约 40 个线程，超出的请求会排队"，说明你真的理解，而不是只会背"自动放进线程池"。

---

## 5. 为什么阻塞调用会"卡死" async def

这是最容易踩的坑，用代码对比：

```python
# ❌ 错误：async def 里调用阻塞代码
import time

@app.get("/slow")
async def slow():
    time.sleep(5)          # 阻塞！占住整个事件循环线程
    return {"ok": True}
```

`time.sleep(5)` 是阻塞调用，它**不会让出事件循环**（它不是 `await`，也不会释放控制权）。这 5 秒内：

- 事件循环线程被这个函数占死；
- **所有**其他请求（包括其他 async 端点）都无法被处理；
- 整个服务表现为"串行卡死"。

正确写法是用 `await asyncio.sleep(5)`（异步 sleep，会让出循环）。

> 这正是 DESIGN.md 里"禁止在 async def 端点里直接调用同步阻塞代码（会导致整个服务串行卡死）"的由来。

---

## 6. 本项目为什么统一用同步 def

**结论先行**：因为本项目到处都是**阻塞调用**，用同步 `def` 让它们自动跑在线程池里，是**最简单、最安全**的选择。

本项目的阻塞调用清单：

| 操作 | 库 | 是否阻塞 |
|------|-----|---------|
| 数据库读写 | `sqlite3` | 阻塞（磁盘 I/O + 锁） |
| 向量检索 | `chromadb` | 阻塞 |
| Embedding 推理 | `sentence-transformers`/`torch` | 阻塞（CPU 计算） |
| LLM 调用 | `openai`（同步客户端） | 阻塞（网络 I/O） |
| 文件读写 | `Path.read_text/write_bytes` | 阻塞（磁盘 I/O） |
| 文件解析 | `pypdf`/`docx2txt` | 阻塞（CPU） |

这些库**绝大多数是同步阻塞的**。如果硬要用 `async def`，就必须把它们全部换成异步版本（异步 DB 驱动、异步 HTTP、异步文件 I/O……），或者手动 `await asyncio.to_thread(...)`，代码复杂度和出错概率都大幅上升。

**统一同步 `def` 的取舍**：

- ✅ 简单、不易错、代码直白；
- ✅ 阻塞调用自动进线程池，不卡事件循环；
- ⚠️ 并发能力受限于线程池大小（约 40），且每个请求都要消耗一个 OS 线程（有内存/上下文切换开销）。

对"个人知识库、单机部署、并发不高"的场景，这个取舍完全正确。

> 延伸：如果要扛**超高并发**（每秒成千上万请求），纯同步 + 线程池会遇到瓶颈，届时应该对真正的 I/O 密集环节改用异步库 + `async def`，CPU 密集环节（embedding/LLM）继续用线程池或独立 worker。

---

## 7. 深挖：本项目的"阻塞调用"到底阻塞在哪

理解"阻塞在哪"能帮你判断哪些环节适合线程、哪些环节其实能并行。

### 7.1 网络 I/O（LLM 调用、OpenAI embedding）

`openai` 同步客户端内部是阻塞的 HTTP 请求（基于 `httpx` 同步模式）。请求发出后，线程在**等服务器返回**，这段时间**会释放 GIL**，别的线程能跑。

**性质**：典型 I/O 阻塞 → 多线程并发效果很好。

### 7.2 磁盘 I/O（SQLite、文件读写）

`sqlite3` 和文件读写是磁盘 I/O，阻塞时线程等待操作系统返回，**同样会释放 GIL**。

**但注意**：SQLite 有自己的一层锁（见 §8），写操作会串行化。

### 7.3 CPU 计算（torch embedding、PDF 解析）

`torch` 推理和 `pypdf` 解析是 CPU 密集计算。torch 底层大量运算在 C++/BLAS 里执行，**部分会释放 GIL**，因此多线程能获得一定并行加速；但 Python 侧调度和纯 Python 解析部分仍受 GIL 限制，**多线程无法线性加速**。

**性质**：CPU 密集 → 多线程收益有限，真正的加速要靠多进程或 GPU。

> 这张"阻塞性质地图"是判断并发方案的地基：I/O 密集 → 线程/异步都好使；CPU 密集 → 线程收益有限，考虑多进程/GPU。

---

## 8. SQLite 并发与锁（本项目最容易忽略的点）

### 8.1 连接管理：为什么本项目"每请求一连接"是安全的

SQLite 的连接对象默认**不能跨线程使用**（`check_same_thread=True`）。本项目 `database.py` 的 `db()` 上下文管理器**每次新建连接、用完即关**：

```python
@contextmanager
def db():
    conn = get_connection()   # 新建连接
    try:
        yield conn
        conn.commit()
    except Exception:
        conn.rollback()
        raise
    finally:
        conn.close()          # 关闭，不跨请求复用
```

好处：**天然规避了"跨线程共用连接"的问题**。每个请求在自己的线程里建连、使用、关闭，互不干扰。

代价：频繁建连有开销（个人场景可忽略）。

### 8.2 SQLite 的锁模型：读并发、写串行

SQLite 是**单写者**数据库：

- **多个读**：可以并发；
- **一个写**：写时独占，其他写会报 `database is locked`；
- **读写混合**：默认 journal 模式下，写会阻塞读。

**对本项目的影响**：并发问答时，每个问答都要写多条消息（`INSERT chat_messages` + `UPDATE chat_sessions`），**写操作会串行化**。如果并发写很多，可能出现 `database is locked` 错误（当前代码没有重试逻辑）。

### 8.3 打开 WAL 提升并发读（改进点）

SQLite 的 **WAL（Write-Ahead Logging，预写日志）** 模式能让"**读不阻塞写、写不阻塞读**"，显著提升读写并发：

```python
conn.execute("PRAGMA journal_mode=WAL")
```

这是本项目一个**容易加分的改进建议**：在高并发或读写混合场景下，把 journal 模式切到 WAL，并给写操作加重试（遇到 `database is locked` 时短暂等待重试）。

---

## 9. 模块级单例的线程安全问题

本项目有两个模块级单例：

```python
# embedding_service.py
_local_model = None          # SentenceTransformer 模型

# retrieval_service.py
_client = None               # chromadb.PersistentClient
```

它们都在首次调用时初始化一次，之后被**多个线程共享**。

- **线程安全吗**：这两个对象本身的设计是可并发调用的（`model.encode` 是只读推理；Chroma 的 PersistentClient 内部对并发读写做了处理），所以共享通常没问题。
- **初始化竞态**：理论上多线程同时首次调用可能触发重复初始化（两个线程都看到 `None` 都去建）。实践中因为初始化发生在启动早期/首个请求，概率低，但严格来说**不是原子的**，可用 `threading.Lock` 或"双重检查"加固。
- **好处**：避免每个请求都重新加载模型（加载极慢）或重建客户端。

> 面试话术：模块级单例能避免重复加载开销，但要注意"懒加载 + 多线程首次访问"的竞态；对只读、可复用的对象（模型推理、持久化客户端）共享是安全的。

---

## 10. 流式 SSE 与并发的交互

这是本项目一个**隐蔽但重要的并发点**。

### 10.1 流式请求会"长期占用"一个线程池线程

流式端点：

```python
@router.post("/api/knowledge-bases/{kb_id}/chat/stream")
def chat_stream(kb_id: str, body: ChatRequest):     # 同步 def
    return EventSourceResponse(chat_service.chat_stream(...))
```

因为端点是**同步 `def`**，它跑在线程池里。而 `EventSourceResponse` 要**逐步消费生成器**、逐条推送 token，这个"逐步"会持续整个 LLM 生成过程（可能几秒到几十秒）。

**后果**：**每个进行中的流式请求，会占用一个线程池线程直到流结束**。如果线程池只有 40 个线程，40 个用户同时开流式问答，第 41 个请求就会**排队等待**，可能超时。

### 10.2 生成器里也有阻塞调用

`chat_service.chat_stream` 生成器内部会：

- `_save_message`（SQLite 写）；
- `embedding_service.embed_one`（torch 推理）；
- `llm_service.chat_stream`（阻塞网络 I/O）。

这些都在线程池线程里执行，因为同步 `def` + 同步生成器会被 Starlette 在线程池里迭代。所以单个流式请求的阻塞不会卡事件循环，但它**持续占用一个线程**。

### 10.3 改进方向

若流式并发是瓶颈，可考虑：
1. 增大线程池容量（权衡内存）；
2. 把流式端点的生成器改成 `async def` 生成器 + 异步 LLM 客户端（`AsyncOpenAI`），让网络等待期间**释放线程**，一个事件循环线程能同时服务大量流式请求；
3. 用 uvicorn 多 worker 横向扩展进程。

---

## 11. 中间件与并发的坑

本项目 `middlewares/auth.py` 注释里点明了一个经典坑：

> 使用纯 ASGI 中间件而非 `BaseHTTPMiddleware`，避免后者缓冲破坏 SSE 流式响应。

`BaseHTTPMiddleware` 是 Starlette 提供的高层中间件，但它为了支持"请求/响应体读取"而**缓冲响应体**。对 SSE 这种"边生成边发送"的流式响应，缓冲会导致：

- 响应被攒起来，流式效果丢失；
- 甚至和后台任务、异常处理产生微妙交互（这是 Starlette 官方也建议谨慎使用的组件）。

所以本项目自己实现了一个**纯 ASGI 中间件**：

```python
class ApiKeyMiddleware:
    async def __call__(self, scope, receive, send):
        if scope["type"] == "http":
            ...  # 校验 X-API-Key
            if 不通过:
                await resp(scope, receive, send)
                return
        await self.app(scope, receive, send)   # 直接透传给下一层
```

纯 ASGI 中间件直接操作 `scope/receive/send`，不缓冲、不拦截流式数据，是流式场景下的正确选择。

---

## 12. 多请求同时到达，实际发生了什么

以"同时来了 3 个问答请求 + 1 个流式问答"为例，画一张时序图：

```
事件循环线程（1 个，负责接收/调度）
   │
   ├─ 收到请求1(同步def) → 交给线程池 worker1
   ├─ 收到请求2(同步def) → 交给线程池 worker2
   ├─ 收到请求3(同步def) → 交给线程池 worker3
   └─ 收到请求4(流式def) → 交给线程池 worker4（长期占用直到流结束）

线程池（默认约 40 个 worker）
   ├─ worker1: embed → 检索 → LLM → 写库 → 返回
   ├─ worker2: embed → 检索 → LLM → 写库 → 返回
   ├─ worker3: embed → 检索 → LLM → 写库 → 返回
   └─ worker4: 持续 yield token ……（占用中）
```

要点：

1. 事件循环线程**从不执行阻塞代码**，只负责收请求、派发任务，所以永远"活着"；
2. 真正的阻塞工作都在 worker 线程里，互不阻塞；
3. **瓶颈在 worker 数量**：超过线程池容量就排队；
4. 所有 worker 共享 SQLite（写串行）和单例模型/客户端（并发读）。

---

## 13. 调优：workers、线程池、WAL

### 13.1 uvicorn 多 worker（多进程）

```bash
uvicorn main:app --workers 4
```

- 每个 worker 是**独立进程**，有**独立的事件循环和线程池**，也**绕过了 GIL**（多进程真并行）；
- 适合 CPU 密集环节（embedding/LLM）想要更多并行吞吐时；
- 代价：每个进程都要**重新加载模型**（torch 模型内存占用 × worker 数），且多进程不能共享单例。

**权衡**：本项目的 embedding 模型较重，多 worker 会成倍吃内存；个人场景单 worker 通常够用。

### 13.2 线程池容量

anyio 默认约 40 线程。若并发高、响应慢，可增大，但要注意每个 OS 线程的内存和上下文切换开销。CPU 密集任务线程开多了反而变慢（GIL + 竞争）。

### 13.3 SQLite 打开 WAL + 写重试

- `PRAGMA journal_mode=WAL`：读不阻塞写、写不阻塞读；
- 写操作加"`database is locked` 重试"逻辑：短暂 sleep 后重试若干次。

这两条能显著提升本项目在高并发写场景下的稳定性。

### 13.4 压测与监控

- 用 `httpx`/`locust`/`ab` 压测 `/health` 和问答接口，观察并发下的延迟和错误；
- 关注线程池耗尽、SQLite 锁、超时三类问题。

---

## 14. 什么时候该用 async def + 异步库

同步 `def` 不是万能解药。以下信号出现时，应转向异步：

1. **I/O 密集且并发极高**：大量网络调用（如调外部 LLM API），同步线程池会先到瓶颈；
2. **需要"等待期间不占线程"**：例如流式响应，异步生成器 + `AsyncOpenAI` 能让一个事件循环线程同时服务大量流；
3. **有成熟的异步库可用**：如 `httpx.AsyncClient`、`AsyncOpenAI`、异步数据库驱动（`aiosqlite`、`asyncpg`）。

### 14.1 异步改造的正确姿势

```python
from openai import AsyncOpenAI

@app.post("/chat")
async def chat(body: ChatRequest):        # 改成 async def
    client = AsyncOpenAI(...)
    resp = await client.chat.completions.create(...)   # await 异步调用
    return resp.choices[0].message.content
```

要点：**async def 里只能 await 异步操作**；如果有无法异步化的阻塞代码（如 torch、pypdf），必须包一层：

```python
import asyncio

result = await asyncio.to_thread(blocking_func, *args)   # 手动丢线程池
```

`asyncio.to_thread` 就是把阻塞函数丢进线程池执行，同时让事件循环不被卡住——这正是"同步 def 自动做的事"，手动版。

### 14.2 本项目最合理的混合方案

如果真要优化本项目并发，最合理的不是"全改异步"，而是**分层**：

- **CPU 密集**（embedding、PDF 解析）：继续线程池或独立 worker/进程，异步也救不了 GIL；
- **网络 I/O**（LLM 调用、流式）：改 `AsyncOpenAI` + `async def` 生成器，收益最大；
- **数据库**：SQLite 本身单写，改 `aiosqlite` 或开 WAL + 重试。

---

## 15. 本项目的并发隐患清单

站在"能审代码"的视角，本项目并发相关的可改进点：

1. **流式请求长期占用线程池线程**（§10）：高并发流式会耗尽 40 个线程。
2. **SQLite 写串行 + 无重试**：并发写可能 `database is locked`（可开 WAL + 加重试）。
3. **单例懒加载非原子**：多线程首次访问可能竞态重复初始化（可用 Lock 加固）。
4. **上传接口一次性读入内存**：`file.file.read()` 对超大文件占用内存（与并发叠加时更明显）。
5. **`background.add_task` 仍占线程**：文档处理是 CPU 密集（embedding），后台任务也跑在线程池里，多个大文档同时上传会挤占线程池，影响在线问答响应。
6. **无连接池/会话复用**：每个请求新建 SQLite 连接、新建 LLM 客户端，高并发下有开销。

---

## 16. 并发专题面试追问集

### Q1. FastAPI 里 `def` 和 `async def` 端点，运行机制有什么区别？
**答**：`async def` 端点直接跑在**事件循环**里，靠 `await` 异步操作实现并发；普通 `def` 端点被 Starlette 通过 `run_in_threadpool` 丢进**线程池**（anyio，默认约 40 线程）执行，阻塞调用不会卡住事件循环，但并发受线程池容量限制。

### Q2. 什么是事件循环？为什么 async def 里写阻塞代码会"卡死"？
**答**：事件循环是单线程调度器，靠协程在 `await` 处挂起/恢复实现并发。阻塞代码不会 `await`、不会让出控制权，会占住事件循环唯一的线程，导致其他所有任务无法调度，服务表现为串行卡死。所以 async 函数里只能用异步操作，阻塞代码要丢线程池。

### Q3. 为什么这个项目所有端点都用同步 `def`？
**答**：因为项目大量使用同步阻塞库（sqlite3、chromadb、torch、openai 同步客户端、文件 I/O）。同步 `def` 会让这些阻塞调用自动跑在线程池，避免卡事件循环，且代码简单不易错。代价是并发上限受线程池容量约束，适合个人/低并发场景。

### Q4. GIL 是什么？它会让多线程完全没用吗？
**答**：GIL 是 CPython 的全局解释器锁，同一时刻只允许一个线程执行 Python 字节码。它让"多线程并行纯 Python 计算"无效，但 I/O 阻塞时线程会释放 GIL，所以**多线程对 I/O 密集型并发仍然有效**；调用会释放 GIL 的 C 扩展（numpy/torch 底层）也能获得一定并行。要真正并行 CPU 计算，需用多进程。

### Q5. 线程池的容量是多少？超过容量会发生什么？
**答**：FastAPI 同步端点用的 anyio 线程池默认约 40 个线程。并发请求超过容量时，多余请求会**排队等待**空闲线程，表现为延迟上升甚至超时。

### Q6. 本项目为什么每个请求新建 SQLite 连接？这安全吗？
**答**：SQLite 连接默认不能跨线程使用，而同步端点跑在多线程里。`db()` 上下文管理器每次新建连接、用完即关，天然规避了"跨线程共用连接"的问题，是安全的。代价是频繁建连的小开销，个人场景可接受。

### Q7. SQLite 的并发特性是什么？怎么提升它的并发？
**答**：SQLite 是单写者数据库——多读可并发，但写是串行的，并发写会 `database is locked`。提升手段：打开 `PRAGMA journal_mode=WAL`（读不阻塞写），并给写操作加"锁冲突重试"逻辑。

### Q8. 模块级单例（embedding 模型、chroma client）在多线程下安全吗？
**答**：这两个对象本身可并发复用（只读推理、持久化客户端内部处理并发），共享是安全的。但"懒加载 + 多线程首次访问"存在初始化竞态（非原子），严格可用 `threading.Lock` 加固。共享单例的最大好处是避免每个请求重复加载模型/重建客户端。

### Q9. 流式 SSE 请求在并发上有什么特殊性？
**答**：同步 `def` 的流式端点跑在线程池里，而 SSE 要"逐步消费生成器"，所以**每个流式请求会长期占用一个线程池线程直到流结束**。高并发流式会耗尽线程池。优化方向：改成 `async def` 生成器 + `AsyncOpenAI`，让网络等待期间释放线程。

### Q10. 为什么鉴权中间件要手写纯 ASGI，而不用 BaseHTTPMiddleware？
**答**：`BaseHTTPMiddleware` 会缓冲响应体，破坏 SSE 流式响应（无法边生成边发送）。纯 ASGI 中间件直接操作 `scope/receive/send`，不缓冲、不拦截流，是流式场景下的正确实现。

### Q11. 如果要提升本项目的并发吞吐，你会做哪些事（按优先级）？
**答**：
1. SQLite 开 WAL + 写重试（低成本高收益）；
2. 把 LLM 调用/流式改成 `AsyncOpenAI` + `async def`（释放线程，收益最大）；
3. 必要时用 uvicorn 多 worker（注意模型内存 × worker 数）；
4. 文档处理从 `background.add_task`（占线程池）迁移到独立任务队列；
5. 大文件上传改流式读写，避免内存峰值；
6. 加并发压测定位真实瓶颈，而非盲调。

### Q12. 一句话总结本项目的并发模型。
**答**：事件循环只做调度、不碰阻塞；所有阻塞工作（SQLite、Chroma、torch、LLM）都通过"同步 `def` + 线程池"异步化执行，简单安全地实现并发，代价是并发上限受线程池（约 40）和 SQLite 单写约束。

---

> 至此，FastAPI 并发从"GIL → 事件循环 → 线程池 → 本项目取舍 → 调优"已完整打通。建议配合《项目分析报告.md》§8（并发约定）和《知识点详解.md》§12 一起复习，再把 §15 的隐患清单逐条想一遍"怎么改"。
