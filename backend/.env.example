# 复制本文件为 .env，并填入你的真实密钥（.env 已被 .gitignore 忽略，不会入库）
# 复制方法：在 VS Code 左侧文件列表里，右键 .env.example → 复制 → 粘贴，改名为 .env

# DeepSeek 的 API Key（去 platform.deepseek.com 获取，形如 sk-xxxx）
OPENAI_API_KEY=sk-你的DeepSeek密钥

# 国内加速：HuggingFace 模型下载走镜像（模型已缓存一般不会再用到，但建议保留）
HF_ENDPOINT=https://hf-mirror.com
