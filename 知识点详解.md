# PersonalKB-QA 知识点详解（小白友好版）

> 本文把项目里涉及的**每一个知识点**拆开讲清楚：它是什么、为什么需要它、在这个项目里具体怎么用、对应哪段代码。建议配合《项目分析报告.md》一起看，先看报告了解全貌，再回来逐个吃透。

---

## 目录

- [第一部分：RAG 核心概念](#第一部分rag-核心概念)
- [第二部分：Python / FastAPI 后端](#第二部分python--fastapi-后端)
- [第三部分：数据存储与数据库](#第三部分数据存储与数据库)
- [第四部分：前端 React / TypeScript](#第四部分前端-react--typescript)
- [第五部分：工程化与安全](#第五部分工程化与安全)

---

## 第一部分：RAG 核心概念

### 1. RAG（检索增强生成）

**是什么**：RAG = Retrieval-Augmented Generation，把"检索"和"生成"两个环节拼起来。流程：先从知识库里检索相关内容，再把检索结果作为上下文喂给大模型，让模型基于这些资料回答问题。

**为什么需要**：
- 大模型不知道你的私有文档；
- 大模型会"幻觉"（一本正经地胡说八道）；
- 大模型有训练截止日期，不知道新知识。

**本项目中**：`chat_service.py` 里的 `chat()` / `_chat_stream_impl()` 就是 RAG 编排——先 `_retrieve()` 检索，再 `_build_prompt()` 拼上下文，最后调 `llm_service` 生成。

---

### 2. Embedding（向量化 / 词嵌入）

**是什么**：把一段文字映射成一串数字（向量），语义相近的文字，向量在空间里也相近。

**为什么需要**：计算机不能直接"理解"文字含义，但可以计算向量的距离/夹角来判断"这两段话像不像"。

**举例**："苹果手机怎么样" 和 "iPhone 好不好用" 语义接近，它们的向量余弦相似度高；而和 "今天天气不错" 的向量相似度低。

**本项目中**：`embedding_service.py` 的 `embed()` / `embed_one()`。两种模式：
- `local`：用 `sentence-transformers` 加载 `text2vec-base-chinese` 中文模型；
- `openai`：调 OpenAI 的 embeddings 接口。

---

### 3. 余弦相似度 与 余弦距离

**是什么**：
- 余弦相似度（cosine similarity）：衡量两个向量**方向**的接近程度，范围 `[-1, 1]`，越接近 1 越相似。
- 余弦距离 = `1 - 余弦相似度`。

**为什么**：向量检索本质上就是"找离问题向量最近的几个文档向量"，用什么"距离"定义很关键。

**本项目中的坑（重点）**：ChromaDB 建 Collection 时指定了 `cosine` 空间，它 `query` 返回的 `distances` 是**余弦距离**而不是相似度，所以代码里要 `similarity = 1 - distance`，再用阈值过滤：

```python
# retrieval_service.py
"similarity": round(1.0 - distances[i], 6)
```

```python
# chat_service.py 里的过滤
threshold = config.get("retrieval.similarity_threshold", 0.5)
return [r for r in results if r["similarity"] >= threshold]
```

> 如果建 Collection 时用的是默认 L2 度量，`distances` 的语义就完全不同，这个 0.5 阈值就会完全错误——这是 DESIGN.md v1.1 重点修正的 bug。

---

### 4. 向量数据库 ChromaDB

**是什么**：专门存向量、并支持"相似向量快速检索"的数据库。本项目用 `PersistentClient`（嵌入式，数据落盘到本地目录），不用单独起服务。

**为什么不用普通数据库**：普通数据库做的是"精确匹配"（`WHERE name = 'x'`），而向量检索是"模糊语义匹配"，需要 ANN（近似最近邻）索引。

**本项目中的用法**（`retrieval_service.py`）：

```python
client = chromadb.PersistentClient(path=persist_dir)
collection = client.get_or_create_collection(
    name=f"kb_{kb_id}",                       # 每个知识库一个 Collection
    metadata={"hnsw:space": "cosine"},        # 显式 cosine 度量
)
```

关键操作：
- **写入** `add_chunks`：`collection.add(ids, embeddings, documents, metadatas)`。**必须显式传 `embeddings=`**，否则 Chroma 会用自己默认模型联网下载，破坏本地一致性。
- **查询** `search`：`collection.query(query_embeddings=[...], n_results=top_k)`。
- **删除**：`collection.delete(where={"document_id": doc_id})`、`client.delete_collection(name)`。

---

### 5. 文档解析（Loaders）

**是什么**：把不同格式的原始文件提取成纯文本。

**本项目支持的格式与解析器**（`document_service.py` 的 `_extract_text`）：

| 格式 | 解析方式 | 依赖 |
|------|---------|------|
| `.txt` / `.md` | `Path.read_text`（直接按文本读） | 无 |
| `.pdf` | `PyPDFLoader`（LangChain 封装） | `pypdf` |
| `.docx` | `Docx2txtLoader` | `docx2txt` |

**为什么 Markdown 用纯文本读**：Markdown 本质就是带 `#`、`*` 等符号的纯文本，不需要 `unstructured` 那种重型解析库（它在 Windows 上难装）。

---

### 6. 文本分块（Chunking）

**是什么**：把一篇长文档切成一段段小文本（chunk），每段分别向量化、分别检索。

**为什么切**：
- Embedding 模型有**最大输入长度**限制，整篇塞不下；
- 检索时希望返回"精准的小片段"而不是整篇文档，这样 Prompt 更聚焦。

**本项目**（`document_service.py` 的 `_split_text`）：

```python
RecursiveCharacterTextSplitter(
    chunk_size=500,        # 每块约 500 字符
    chunk_overlap=50,      # 相邻块重叠 50 字符
    separators=["\n\n", "\n", "。", "！", "？", "；", "，", " ", ""],
)
```

三个概念：
- **chunk_size**：每块的目标长度；
- **chunk_overlap**：块之间保留重叠，避免把一个完整语义的句子从中间切断，导致检索时上下文丢失；
- **separators**：按优先级从高到低找切分点。**中文标点（。！？；，）是关键**，默认分隔符是英文导向的，对中文切分质量差。

---

### 7. 检索参数：top_k 与相似度阈值

- **top_k**：检索返回"最相似的 K 个"片段。本项目默认 5。
- **similarity_threshold**：相似度阈值，低于它的结果直接丢弃。本项目 `config.yaml` 配了 0.4（即丢掉余弦相似度 < 0.4 的片段）——因为 `text2vec-base-chinese` 的相似度分数整体偏低，0.5 会漏掉不少相关内容，调到 0.4 召回更稳（代码里 `config.get(..., 0.5)` 的 0.5 只是"读不到配置时"的兜底默认值）。

**空检索兜底**：过滤后 0 条时，直接返回"知识库中暂无相关信息，无法回答该问题"，**不调用 LLM**（`chat_service.py` 里 `NO_INFO_ANSWER`）。

---

### 8. Prompt 工程（提示词构建）

**是什么**：把检索到的资料和用户问题按模板拼成一段话，作为 LLM 的输入。

**本项目模板**（`chat_service.py` 的 `SYSTEM_PROMPT`）：

```python
"""你是一个知识库助手。请根据以下参考资料回答用户问题。
如果参考资料中没有相关信息，请如实告知。

参考资料：
{chunks}

用户问题：{question}

请回答："""
```

`_build_prompt()` 会把检索到的 chunk 编号拼进 `{chunks}`：

```python
chunk_text = "\n\n".join(f"[{i + 1}] {c['content']}" for i, c in enumerate(chunks))
```

**要点**：明确告诉模型"只能根据参考资料回答、没有就如实说"，这是抑制幻觉的常用手段。

---

### 9. LLM 调用（OpenAI 兼容协议 + DeepSeek / Ollama）

**是什么**：调用大模型生成文本的接口。

**本项目**（`llm_service.py`）：
- 统一用 `openai` 这个 SDK 的 `OpenAI` 客户端；
- **默认 OpenAI 模式（DeepSeek）**：`base_url=https://api.deepseek.com/v1`、`model=deepseek-chat`。DeepSeek 提供了 OpenAI 兼容接口，所以无需换 SDK；
- **Ollama 模式**：Ollama 本地暴露了 OpenAI 兼容的 `/v1` 端点，所以代码里：
  ```python
  base_url = "http://localhost:11434".rstrip("/") + "/v1"
  api_key = "ollama"   # Ollama 不需要真 key，随便填
  ```
  这样多种模式**复用同一套调用代码**，这是"适配器/统一接口"思想的体现。换 Kimi/智谱/通义等，只需改 `base_url` 和 `model`。

- **key 前置校验（新增）**：`openai` 模式下若 `api_key` 为空或仍含 `${`（环境变量没展开），直接抛 `ValueError` 提示设置 `OPENAI_API_KEY`，把配置错误暴露在调用前，而不是等 LLM 返回晦涩的 401。
- **流式生成** `chat_stream`：`stream=True`，逐 chunk 取 `chunk.choices[0].delta.content` 并 `yield`。

---

### 10. SSE（Server-Sent Events，服务端推送事件）

**是什么**：一种让服务器**持续向浏览器推送数据**的协议，单向（服务器→客户端），基于普通 HTTP，用 `text/event-stream` 格式，每行以 `data:` 开头。

**为什么不用 WebSocket**：SSE 更轻量、天然适配 HTTP、只做单向推送，聊天流式输出用 SSE 足够。

**为什么 axios 不行**：axios 会等整个响应体收完才返回，拿不到"逐条"数据；必须用支持流式读的 `fetch` + `ReadableStream` 或 `@microsoft/fetch-event-source`。

**后端**（`routers/chat.py`）：

```python
return EventSourceResponse(chat_service.chat_stream(...))
```

`chat_stream` 是一个**生成器**，`yield` 出来的 dict 形如 `{"event": "token", "data": '{"type":"token","content":"你好"}'}`。

**前端**（`api/stream.ts`）：用 `fetchEventSource` 监听 `onmessage`，解析 `msg.type` 分发给不同回调。

**事件类型**：`thinking`（开始）、`token`（逐字）、`sources`（引用来源）、`done`（结束，带 message_id/session_id）、`error`（出错）。

---

## 第二部分：Python / FastAPI 后端

### 11. FastAPI 是什么

**是什么**：Python 的现代 Web 框架，基于类型注解（type hints）自动做请求校验、序列化、生成接口文档（`/docs` Swagger）。

**本项目入口** `main.py`：

```python
app = FastAPI(title="PersonalKB-QA", version="1.1.0", lifespan=lifespan)
```

---

### 12. 同步 `def` vs `async def`（本项目最重要的并发知识点）

**是什么**：
- `async def` 端点跑在事件循环里，如果里面调了阻塞代码（数据库、网络、torch），会把整个事件循环卡死，所有请求都动不了。
- 同步 `def` 端点，FastAPI 会**自动放进线程池**执行，阻塞调用不会卡住事件循环。

**本项目约定**：**所有端点统一用同步 `def`**，让 ChromaDB、SQLite、openai、torch 这些阻塞调用都跑在线程池里。

```python
@router.post("")
def create_kb(body: KnowledgeBaseCreate):   # 注意是 def 不是 async def
    ...
```

> 面试高频题："为什么 FastAPI 项目里你全部用同步 def？" —— 答：因为项目里大量阻塞调用（SQLite、ChromaDB、torch embedding、HTTP 请求），同步 def 会被 FastAPI 自动放进线程池，避免阻塞事件循环；而 async def 里写阻塞调用会导致整个服务串行卡死。

---

### 13. lifespan（应用生命周期）

**是什么**：FastAPI 新推荐的应用启动/关闭钩子，用 `@asynccontextmanager` 装饰。

```python
@asynccontextmanager
async def lifespan(app: FastAPI):
    init_db()          # 启动时建表
    yield
```

它替代了旧版（已废弃）的 `@app.on_event("startup")`。

---

### 14. 中间件（Middleware）

**是什么**：在请求进入路由之前、响应返回之前插入的一层处理逻辑（类似"关卡"）。可以做鉴权、CORS、日志等。

**本项目两种中间件**：
1. `CORSMiddleware`（`main.py`）：处理跨域，`allow_origins` 白名单。
2. `ApiKeyMiddleware`（`middlewares/auth.py`）：可选 API Key 鉴权。

**关键细节**：鉴权中间件是**纯 ASGI 中间件**（手写 `__call__(scope, receive, send)`），而不是 Starlette 的 `BaseHTTPMiddleware`。因为 `BaseHTTPMiddleware` 会缓冲响应体，**会破坏 SSE 流式响应**。

---

### 15. CORS（跨域资源共享）

**是什么**：浏览器的同源策略默认禁止跨域请求。前端跑在 `localhost:5173`，后端在 `127.0.0.1:8000`，属于跨域，需要后端允许。

**本项目**：
- 后端 `main.py` 用 `CORSMiddleware` 允许 `http://localhost:5173`；
- 前端 `vite.config.ts` 用 proxy 把 `/api` 转发到 `127.0.0.1:8000`（开发期用 proxy，**浏览器根本感知不到跨域**，因为请求还是发到 5173 自己）。

> 面试题："为什么既配了 Vite proxy 又配了 CORS？" —— proxy 是开发期首选（请求同源，简单干净）；CORS 作为兜底（生产环境前端静态资源和后端不同源时用）。

---

### 16. 异常处理与统一响应

**是什么**：FastAPI 里抛 `HTTPException` 会返回对应状态码，但默认格式不是我们想要的 `{code,data,message}`，所以注册异常处理器统一包裹。

`main.py` 里三个 `@app.exception_handler`：
- `HTTPException` → 状态码不变，body 用 `fail(detail, status_code)`；
- `RequestValidationError`（Pydantic 校验失败）→ 422；
- `Exception`（兜底）→ 500。

**统一响应格式**（`models/common.py`）：

```python
def ok(data=None):  return {"code": 0, "data": data, "message": "success"}
def fail(message, code=-1): return {"code": code, "data": None, "message": message}
```

`code == 0` 表示成功，`code != 0` 表示失败（HTTP 错误时 code 取状态码）。

---

### 17. Pydantic（数据校验）

**是什么**：用 Python 类型注解定义数据结构，自动做校验和序列化。

**本项目**（`models/knowledge_base.py`）：

```python
class KnowledgeBaseCreate(BaseModel):
    name: str = Field(..., min_length=1, max_length=200)  # 必填，长度 1~200
    description: str = ""
```

`Field(...)` 里的 `...` 表示"必填"；不符合约束的请求会被 FastAPI 自动拦截返回 422。

**注意区分**：这里的 `models/` 是 Pydantic 的 schema（请求/响应模型），**不是**数据库 ORM 模型——因为项目不用 ORM。

---

### 18. BackgroundTasks（后台任务）

**是什么**：FastAPI 提供的"响应返回后继续执行任务"的机制。上传文档时，接口先返回 Document（status=processing），真正的解析/向量化交给后台任务跑。

**本项目**（`routers/document.py`）：

```python
background.add_task(
    document_service.process_document,
    doc["id"], kb_id, doc["file_path"], file_type, doc["filename"],
)
```

> 注意：`BackgroundTasks` 会在响应发送**之后**执行，但它仍然占着同一个请求的线程（不是完全异步的独立 worker）。真正长任务建议用 Celery 等任务队列，但对本项目"个人知识库、文档不大"的场景够用。

---

### 19. 生成器（Generator）与 `yield`

**是什么**：函数里用 `yield` 返回值，调用时不一次性执行完，而是"逐个产出"，节省内存、支持流式。

**本项目**：`llm_service.chat_stream` 和 `chat_service.chat_stream` 都是生成器，`EventSourceResponse` 会逐个消费它们 `yield` 出的事件。

---

### 20. 惰性导入（Lazy Import）

**是什么**：把 `import` 语句写在函数体里，而不是文件顶部，只有在真正用到时才导入。

**为什么**：torch、chromadb、langchain 这些依赖又大又慢，放到顶部会让"只做知识库 CRUD"也必须先装上它们。

**本项目**：`embedding_service._get_local_model` 里 `from sentence_transformers import SentenceTransformer`、`retrieval_service.get_client` 里 `import chromadb` 都是惰性导入。这样 `/health` 和 CRUD 在没有重依赖时也能跑，问答/文档处理才报清晰错误。

---

## 第三部分：数据存储与数据库

### 21. SQLite 与标准库 sqlite3

**是什么**：轻量级嵌入式关系数据库，数据就是一个文件，无需单独服务，非常适合本地小项目。

**本项目**用 Python 标准库 `sqlite3`（不用 SQLAlchemy ORM）：

```python
conn = sqlite3.connect(str(_sqlite_path()))
conn.row_factory = sqlite3.Row   # 让结果能按列名访问
conn.execute("PRAGMA foreign_keys = ON")
```

---

### 22. 外键与 ON DELETE CASCADE

**是什么**：`documents.kb_id` 引用 `knowledge_bases.id`。`ON DELETE CASCADE` 表示"删父表记录时，自动删掉引用它的子表记录"。

**本项目**：删知识库时，只需 `DELETE FROM knowledge_bases WHERE id=?`，documents/sessions/messages 会靠外键自动级联删除。

**关键坑**：**SQLite 默认不启用外键约束**，必须 `PRAGMA foreign_keys = ON`（每个连接都要设），否则 CASCADE 不生效。`database.py` 里就是这么做的。

---

### 23. 参数化查询（防 SQL 注入）

**是什么**：用 `?` 占位符传参数，而不是把变量直接拼进 SQL 字符串。

```python
conn.execute("SELECT * FROM knowledge_bases WHERE id = ?", (kb_id,))   # 安全
# 反面例子（危险）：
# conn.execute(f"SELECT * FROM knowledge_bases WHERE id = '{kb_id}'")
```

**为什么**：直接拼接会让恶意输入变成 SQL 代码被执行（SQL 注入）。参数化查询把"代码"和"数据"分开，数据库驱动会安全转义。

---

### 24. 事务与上下文管理器（contextmanager）

**是什么**：事务保证一组操作"要么全部成功、要么全部回滚"。

**本项目**（`database.py`）：

```python
@contextmanager
def db():
    conn = get_connection()
    try:
        yield conn
        conn.commit()      # 正常则提交
    except Exception:
        conn.rollback()    # 异常则回滚
        raise
    finally:
        conn.close()
```

`with db() as conn:` 就是靠这个 `@contextmanager` 实现"进入时建连接、退出时提交/回滚/关闭"。

---

### 25. JSON 字段存储 list

**是什么**：SQLite 不能直接存 Python list，所以 `sources` 字段存 JSON 字符串。

**本项目**（`chat_service.py`）：
- 写：`json.dumps(sources, ensure_ascii=False)`；
- 读：`json.loads(...)`。

---

### 26. 索引（Index）

**是什么**：给常用查询的列建索引，加速查找（用空间换时间）。

**本项目**（`database.py` DDL）：

```sql
CREATE INDEX idx_documents_kb ON documents(kb_id);
CREATE INDEX idx_sessions_kb ON chat_sessions(kb_id);
CREATE INDEX idx_messages_session ON chat_messages(session_id);
```

---

## 第四部分：前端 React / TypeScript

### 27. React 函数组件与 Hooks

**是什么**：React 用组件构建 UI；Hooks 让函数组件拥有"状态"和"副作用"能力。

本项目用到的核心 Hooks：
- **`useState`**：组件状态。如 `const [kbs, setKbs] = useState<KnowledgeBase[]>([])`。
- **`useEffect`**：副作用（数据加载、定时器、清理）。如页面加载时 `load()`。
- **`useRef`**：跨渲染保存"可变值"而不触发重渲染。
- **`useParams` / `useNavigate` / `useSearchParams`**：react-router 的路由工具。

---

### 28. useRef 的"双缓冲"技巧（流式渲染关键）

**背景**：`ChatPage` 里 `onToken` 回调要不断累加文字。如果直接 `setStreamingText(streamingText + t)`，由于闭包捕获的 `streamingText` 是"旧值"，连续快速回调会互相覆盖，丢失字符。

**解法**：用 `useRef` 存实时累积值，state 只用于触发渲染：

```tsx
const streamingTextRef = useRef('');
// 回调里：
streamingTextRef.current += t;          // ref 永远是最新值
setStreamingText(streamingTextRef.current);  // 触发 UI 更新
```

> 面试题："为什么流式输出里 state 和 ref 两个都用了？" —— state 负责渲染，ref 负责在异步回调里保存最新累积值，规避闭包陷阱。

---

### 29. 乐观渲染（Optimistic UI）

**是什么**：用户发消息后，不等后端确认，先把用户消息"乐观地"显示出来，提升体验。

**本项目**（`ChatPage.handleSend`）：

```tsx
const userMsg: ChatMessage = { id: `local-${Date.now()}`, role: 'user', content: text, ... };
setMessages(prev => [...prev, userMsg]);
```

---

### 30. axios 拦截器（Interceptor）

**是什么**：在请求发出前/响应返回后统一做处理的钩子。

**本项目**（`api/client.ts`）响应拦截器统一解包 `{code, data, message}`：

```tsx
client.interceptors.response.use(
  (response) => {
    const body = response.data;
    if (body && 'code' in body) {
      if (body.code !== 0) return Promise.reject(new Error(body.message));
      return body.data;    // 只把 data 返回给调用方
    }
    return body;
  },
  (error) => { ... }
);
```

好处：业务代码里 `listKnowledgeBases()` 直接拿到 `KnowledgeBase[]`，不用每次手动解包、判 code。

---

### 31. FormData 与文件上传

**是什么**：浏览器上传文件的格式，`multipart/form-data`。

**本项目**（`api/document.ts`）：

```tsx
const fd = new FormData();
fd.append('file', file);
http.post(`/knowledge-bases/${kbId}/documents`, fd);  // 不手动设 Content-Type
```

**关键点**：不要手动设置 `Content-Type`，让 axios 自动生成 multipart 边界（boundary），否则文件传不完整。

---

### 32. 轮询（Polling）

**是什么**：每隔固定时间主动请求一次，刷新状态。

**本项目**（`KnowledgeBaseDetail`）：只要还有文档处于 `processing`，就每 2 秒 `load()` 一次，处理完自动停：

```tsx
const hasProcessing = docs.some(d => d.status === 'processing');
useEffect(() => {
  if (!hasProcessing) return;
  const t = setInterval(load, 2000);
  return () => clearInterval(t);   // 清理定时器
}, [hasProcessing]);
```

---

### 33. TypeScript 类型系统

**是什么**：给 JS 加上静态类型，编译期发现错误、提供 IDE 提示。

**本项目**：`types/index.ts` 定义了 `KnowledgeBase`、`DocumentItem`、`ChatMessage`、`Source`、`ChatSession` 等接口，后端 Pydantic 模型一一对应。

**泛型**：`http.get<T>(url)` 让每个 API 调用都能指定返回类型：

```tsx
export function listKnowledgeBases(): Promise<KnowledgeBase[]> {
  return http.get('/knowledge-bases');
}
```

---

### 34. React Router（路由）

**是什么**：根据 URL 渲染不同页面，管理跳转。

**本项目**：
- `<BrowserRouter>` + `<Routes>` + `<Route>` 定义路由表；
- `:id` 是**动态路由参数**（`useParams` 取）；
- `<Navigate to="..." replace>` 重定向；
- 嵌套路由 + `<Outlet>`：Layout 是外壳，子页面渲染在 `<Outlet>` 位置；
- `useSearchParams` 管理 URL 查询参数（如 `?session=xxx`）。

---

## 第五部分：工程化与安全

### 35. 配置管理（YAML + 环境变量）

**是什么**：把可变配置（模型名、阈值、端口、密钥）抽到配置文件，代码里不写死。

**本项目** `config.yaml` + `config.py`：
- `Config.get("retrieval.top_k")` 点分路径取值；
- `${OPENAI_API_KEY}` 从环境变量读取，避免密钥写进 YAML 被提交到 git；
- **`.env` 文件支持（新增）**：`_load_dotenv()` 先从 `backend/.env` 读取 `KEY=value` 到环境变量（**不覆盖已存在的变量**），再展开 `${...}`。这样密钥写进 gitignore 的 `.env`，启动时自动加载，不用每次手动 export；
- `resolve_path` 把相对路径转成基于 backend 目录的绝对路径。

---

### 36. 文件上传安全（三条防线）

1. **扩展名白名单**：只允许 `pdf/txt/md/docx`。
2. **魔数（Magic Bytes）校验**：不只看扩展名，还检查文件头字节——PDF 以 `%PDF` 开头、DOCX 以 `PK\x03\x04` 开头（`file_utils.MAGIC_BYTES`）。
3. **文件名清洗 + 路径穿越防护**：`sanitize_filename` 取 `basename`、过滤 `../` 和非法字符，落盘用 `{uuid}_{安全名}`，防止攻击者用 `../../etc/passwd` 覆盖任意文件。

---

### 37. 可选 API Key 鉴权

**是什么**：请求头带 `X-API-Key`，服务端校验，不匹配返回 401。

**本项目**：`security.api_key` 为空则禁用；启用后中间件校验 `/api/*`。`OPTIONS`（CORS 预检）跳过。

**配套安全**：默认绑定 `127.0.0.1`（只本机可访问）；要局域网共享时改 `0.0.0.0` 并开启鉴权。

---

### 38. 依赖管理与虚拟环境

**是什么**：
- `requirements.txt` 记录 Python 依赖及版本；
- 虚拟环境（`.venv`）隔离每个项目的依赖，避免互相污染。

**本项目**依赖分三层：核心轻量依赖、问答依赖、RAG 重依赖（torch ~2GB）。用 `>=` 而非精确锁定 RAG 栈，是因为 Python 3.13 需要更新版本。

---

### 39. 前端构建工具 Vite

**是什么**：新一代前端构建工具，开发时极快热更新，生产时打包优化。

**本项目** `vite.config.ts`：
- `@vitejs/plugin-react` 支持 React；
- dev server 端口 5173；
- `/api` 代理到 `http://127.0.0.1:8000`。

---

### 40. 三层架构 / 分层思想（通用工程能力）

**是什么**：把代码按职责分层，本项目是 `Router → Service → Model`（+ 底层 utils/middlewares）。

**为什么**：
- 职责单一，读代码清晰；
- 业务逻辑集中在 service，可单独单元测试；
- 换实现（如换向量库、换 LLM）只改 service 内部，不影响路由和前端。

---

## 学习路径建议

按下面的顺序学，从"看得懂"到"讲得出"：

1. **先跑起来**：装轻量依赖跑后端骨架，前端 `npm run dev`，访问 `/docs` 看接口。
2. **读懂一条主链路**：从 `routers/chat.py` → `chat_service.py` → `retrieval_service.py` / `llm_service.py`，把 RAG 流程在纸上画一遍。
3. **逐个知识点**：对照本文每个知识点回到代码里找对应位置，做到"知道它在哪、为什么这么写"。
4. **动手改**：改改 `chunk_size`、`top_k`、`similarity_threshold`，观察检索结果变化——这是理解 RAG 参数最快的方式。
5. **能讲出来**：用《面试题与答案解析.md》自测，做到能脱离代码讲清每个概念。

---

> 下一篇请看《面试题与答案解析.md》，按 5 大类整理了面试高频题 + 详解答案。
