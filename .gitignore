# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# 运行时数据（向量库 / 上传文件 / sqlite）
backend/data/
# 公网演示环境的数据目录（由演示配置使用，重新构建即可再生）
backend/data-demo/

# 模型缓存（fastembed ONNX 约 90MB，代码按需下载，不入库）
.cache/

# 密钥 / 环境变量（保留 .env.example 模板）
.env
.env.*
!.env.example

# Node
node_modules/
dist/
.vite/

# TypeScript 编译产物（tsc -b 生成）
*.tsbuildinfo
vite.config.js
vite.config.d.ts
vite.config.js.timestamp-*.mjs

# IDE / OS
.idea/
.vscode/
.DS_Store

# 本地助手工作目录（笔记 / 文档构建产物，不入库）
.workbuddy/

# 日志
*.log
