# PersonalKB-QA 云服务器部署指南

目标：把项目部署到云服务器，手机浏览器直接访问你的 RAG 知识库。

---

## 一、服务器选型

本项目后端用 `sentence-transformers` 本地向量模型，加载时需要约 1.5-2GB 内存（torch + 模型），所以服务器配置建议：

| 配置 | 说明 |
|------|------|
| CPU | 2 核起步 |
| 内存 | **4GB 起步**（2GB 会 OOM，模型加载就很紧张） |
| 系统盘 | 40GB（torch + chromadb + 模型缓存约 8GB） |
| 系统 | Ubuntu 22.04 LTS（推荐）或 CentOS 8 |
| 带宽 | 按量计费 / 5Mbps 起步 |

推荐选择（国内访问快）：
- **阿里云轻量应用服务器** — 新用户有折扣，2核4G 约 60-100元/年
- **腾讯云轻量应用服务器** — 同价位
- **华为云 Flexus** — 也有学生/新用户优惠

> 不建议用 2GB 内存的服务器，模型加载时基本会内存不足。如果预算有限，可以把 embedding 改成 OpenAI 兼容 API（改 config.yaml 的 `embedding.mode: openai`），这样 2GB 也够，但每次检索都要调 API。

---

## 二、本地准备：构建前端 + 调整配置

### 1. 构建前端

在本地 Windows 项目目录执行：

```bash
cd d:\AI-Study\RAG\frontend
npm install
npm run build
```

构建完成后 `frontend/dist` 目录就是前端产物，后端会自动托管它。

### 2. 准备配置文件

把 `backend/.env.example` 复制为 `backend/.env`，填入你的 DeepSeek API Key：

```env
OPENAI_API_KEY=sk-你的真实密钥
HF_ENDPOINT=https://hf-mirror.com
```

> `.env` 已在 `.gitignore` 中，不会上传到 git。部署时手动放到服务器。

### 3. 修改 config.yaml（关键）

`backend/config.yaml` 需要改两处：

```yaml
server:
  host: 0.0.0.0              # ← 从 127.0.0.1 改为 0.0.0.0，允许外部访问
  port: 8000
  cors_origins: ["*"]        # ← 生产环境建议改成你的域名，临时调试用 *
  serve_frontend: true
```

---

## 三、上传项目到服务器

### 方式 A：用 scp 直接传（简单）

在本地 Windows 打开 PowerShell（需要已安装 scp，Win10 自带）：

```powershell
# 先在本地打包（排除 node_modules 等大目录）
cd d:\AI-Study\RAG
# 用 7z 或系统压缩，排除不需要的目录
# 然后上传
scp -r backend frontend root@你的服务器IP:/opt/personalkb/
scp backend/.env root@你的服务器IP:/opt/personalkb/backend/.env
```

### 方式 B：用 Git 仓库（推荐，方便后续更新）

1. 在 GitHub/Gitee 创建私有仓库
2. 本地 `git push`
3. 服务器 `git clone`

```bash
# 服务器上
cd /opt
git clone https://github.com/你的用户名/personalkb-qa.git personalkb
cd personalkb
# .env 不入库，单独创建
nano backend/.env
# 粘贴你的 .env 内容，保存退出（Ctrl+O, Enter, Ctrl+X）
```

> 推荐方式 B：以后改了代码，服务器 `git pull` 就能更新，不用重复传文件。

---

## 四、服务器环境配置

SSH 登录服务器后执行以下步骤：

### 1. 安装系统依赖

```bash
apt update && apt install -y python3 python3-venv python3-pip nodejs npm nginx
```

### 2. 创建虚拟环境 + 安装后端依赖

```bash
cd /opt/personalkb/backend
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

> 这步耗时较长（torch 约 2GB 下载），耐心等待。如果服务器在国内，pip 可换源加速：
> `pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple`

### 3. 构建前端（如果没在本地构建）

```bash
cd /opt/personalkb/frontend
npm install
npm run build
```

### 4. 创建数据目录

```bash
mkdir -p /opt/personalkb/backend/data/chroma
mkdir -p /opt/personalkb/backend/data/uploads
```

### 5. 测试启动

```bash
cd /opt/personalkb/backend
source .venv/bin/activate
python run.py
```

看到 `PersonalKB-QA 启动中：http://0.0.0.0:8000` 就说明成功了。先用 `Ctrl+C` 停掉，下面配置常驻服务。

---

## 五、开放端口 + 安全组

### 1. 云平台安全组

在阿里云/腾讯云控制台找到你的服务器实例 → **防火墙/安全组** → 添加规则：

| 方向 | 端口 | 协议 | 说明 |
|------|------|------|------|
| 入站 | 8000 | TCP | 后端服务 |
| 入站 | 80 | TCP | Nginx HTTP（配域名时用） |
| 入站 | 443 | TCP | Nginx HTTPS（配域名时用） |

### 2. 服务器防火墙

```bash
ufw allow 8000/tcp
ufw allow 80/tcp
ufw allow 443/tcp
ufw enable
```

此时用手机浏览器访问 `http://你的服务器IP:8000` 应该就能看到页面了。

> 如果打不开，先检查：1) 服务是否在运行 2) 安全组是否放行 3) `config.yaml` 的 host 是否为 0.0.0.0

---

## 六、配置常驻服务（systemd）

让服务开机自启、崩溃自动重启：

```bash
cat > /etc/systemd/system/personalkb.service << 'EOF'
[Unit]
Description=PersonalKB-QA RAG Service
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/opt/personalkb/backend
Environment="PATH=/opt/personalkb/backend/.venv/bin"
ExecStart=/opt/personalkb/backend/.venv/bin/python run.py
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
EOF

systemctl daemon-reload
systemctl enable personalkb
systemctl start personalkb

# 查看状态
systemctl status personalkb
# 查看日志
journalctl -u personalkb -f
```

---

## 七、（推荐）Nginx 反向代理 + HTTPS

直接用 IP+端口访问可以，但有两个问题：
1. 浏览器会提示"不安全"
2. 手机网络可能封锁非标准端口

配域名 + HTTPS 解决这两个问题。

### 1. 买个域名 + DNS 解析

在阿里云/腾讯云买个域名（或用已有的），添加 A 记录指向服务器 IP。

### 2. 配置 Nginx

```bash
cat > /etc/nginx/sites-available/personalkb << 'EOF'
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # SSE 流式响应必须加这三行
        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 300s;
    }
}
EOF

ln -s /etc/nginx/sites-available/personalkb /etc/nginx/sites-enabled/
nginx -t
systemctl reload nginx
```

> `proxy_buffering off` 很关键 — RAG 的流式回答（SSE）如果不关缓冲，手机上会等很久才一次性弹出全部内容。

### 3. 申请免费 HTTPS 证书

```bash
apt install -y certbot python3-certbot-nginx
certbot --nginx -d your-domain.com
```

按提示输入邮箱、同意条款，完成后 Nginx 会自动配置好 HTTPS，手机访问 `https://your-domain.com` 就是绿色小锁了。

---

## 八、手机访问

完成以上步骤后：

| 方式 | 地址 | 适合场景 |
|------|------|----------|
| 快速调试 | `http://服务器IP:8000` | 临时试用，无域名 |
| 正式使用 | `https://your-domain.com` | 长期使用，安全 |

手机浏览器打开后可以"添加到主屏幕"，会像 App 一样有图标。

---

## 九、安全加固（重要）

服务器暴露在公网，务必做这几点：

```yaml
# config.yaml — 启用 API Key 鉴权
security:
  api_key: "你自己设一个随机字符串"
```

```bash
# 限制 SSH 登录（可选但推荐）
# 1. 改 SSH 端口
nano /etc/ssh/sshd_config
# 找到 Port 22 改成 Port 2222
systemctl restart sshd

# 2. 禁用密码登录，只用密钥
# PasswordAuthentication no
```

启用 `api_key` 后，前端请求会自动带 `X-API-Key` 头（代码已实现，见 `middlewares/auth.py`），别人扫到你的端口也进不来。

---

## 快速排查清单

部署后访问不了，按这个顺序排查：

1. 服务在运行吗？ → `systemctl status personalkb`
2. 端口监听了吗？ → `ss -tlnp | grep 8000`
3. 安全组放行了吗？ → 云平台控制台检查
4. host 是 0.0.0.0 吗？ → 检查 config.yaml
5. Nginx 配置对吗？ → `nginx -t`
6. 防火墙放行了吗？ → `ufw status`
