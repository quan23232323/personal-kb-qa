# PersonalKB-QA 项目分析报告

> 本文档基于项目实际代码（`backend/` 与 `frontend/`）与 `DESIGN.md`、`README.md` 通读整理而成，目标是让一个"小白"也能从头看懂这个项目在做什么、怎么做的、为什么要这么做。

---

## 0. 一句话概述

**PersonalKB-QA** 是一个**本地私有化部署的 RAG 知识库问答系统**：用户上传文档（PDF/TXT/Markdown/DOCX）→ 系统解析并分块 → 向量化存入向量库 → 用户用自然语言提问 → 系统从知识库里**语义检索**最相关的片段，拼进提示词交给大模型（LLM）生成带"引用来源"的回答，并支持**流式输出**。

一句话串起核心：**"先检索，后生成"**——这就是 RAG（Retrieval-Augmented Generation，检索增强生成）。

---

## 1. 项目定位与核心价值

| 维度 | 说明 |
|------|------|
| 解决的问题 | 让大模型能"回答你自己文档里的内容"，而不是只靠模型训练时的通用知识 |
| 为什么需要 RAG | 大模型（1）不知道你的私有资料；（2）会"幻觉"编造；（3）有知识截止日期。RAG 把真实资料喂给它，大幅降低幻觉 |
| 部署形态 | 完全本地运行，数据不出本机（隐私友好） |
| 目标用户 | 个人、小型团队，需要一个"私人 AI 助手" |

---

## 2. 技术栈总览

| 层级 | 技术 | 作用 |
|------|------|------|
| 前端框架 | React 18 + TypeScript | 构建单页应用（SPA） |
| UI 组件库 | Ant Design 5 (`antd`) | 现成的按钮/卡片/上传/弹窗组件 |
| 构建工具 | Vite | 极快的开发服务器与打包 |
| 前端路由 | react-router-dom v6 | 页面跳转与 URL 管理 |
| 后端框架 | Python FastAPI | 高性能 Web 框架，自动生成接口文档 |
| 服务器 | uvicorn | 跑 FastAPI 应用的 ASGI 服务器 |
| 向量数据库 | ChromaDB（`PersistentClient` 嵌入式） | 存向量 + 做相似度检索 |
| Embedding 模型 | sentence-transformers（`text2vec-base-chinese`）或 OpenAI | 把文本变成向量 |
| 大模型 LLM | DeepSeek（默认）或 OpenAI 兼容 API / Ollama | 生成最终回答 |
| 文档解析 | LangChain Community Loaders | 读 PDF/TXT/MD/DOCX |
| 文本分块 | LangChain `RecursiveCharacterTextSplitter` | 把长文本切成小段 |
| 关系型存储 | SQLite（标准库 `sqlite3`，**无 ORM**） | 存知识库/文档/会话/消息元数据 |
| 数据校验 | Pydantic v2 | 请求/响应模型定义与校验 |
| 流式输出 | SSE（`sse-starlette` + `@microsoft/fetch-event-source`） | 打字机式逐字输出 |

> **两个刻意"反主流"的选择**（面试常问为什么）：
> 1. **不用 ORM（如 SQLAlchemy）**：表只有 4 张、结构简单，标准库 `sqlite3` + 手写 SQL 更轻、零依赖、且和 DDL 完全一致。
> 2. **端点统一用同步 `def` 而不是 `async def`**：因为 ChromaDB、SQLite、torch、openai 这些调用都是**阻塞的**，FastAPI 会把同步端点自动丢进线程池跑，避免阻塞事件循环。详见 §8。

---

## 3. 系统架构

```
┌──────────────────────────────────────────────────────┐
│              前端 (React + Ant Design)                │
│   知识库管理 / 文档上传 / 问答对话（流式渲染）           │
└──────────────────────┬───────────────────────────────┘
                       │ HTTP REST API + SSE
                       │ (开发环境走 Vite proxy，生产走 CORS)
┌──────────────────────┴───────────────────────────────┐
│                  后端 (FastAPI)                        │
│                                                        │
│  Router 层（校验参数/状态码） → Service 层（业务逻辑）    │
│                                                        │
│  ┌─────────┐ ┌─────────┐ ┌──────────┐ ┌──────────┐   │
│  │文档处理  │ │向量化    │ │检索引擎   │ │LLM 调用   │   │
│  └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬──────┘   │
│       └─────────────┴─────────────┴─────────────┘      │
│                          │                              │
│  ┌───────────────────────┴────────────────────────┐    │
│  │  数据层                                          │    │
│  │  ChromaDB(向量)   SQLite(元数据)   文件系统(原始文件)│   │
│  └──────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────┘
```

**三层职责划分**（这是工程化的核心思想，务必理解）：

- **Router（路由层）**：只做参数校验、HTTP 状态码、调 Service，不写业务逻辑。
- **Service（服务层）**：真正的业务逻辑（CRUD、检索、Prompt 组装等）。
- **Model（模型层）**：Pydantic 数据模型，定义字段与类型。

分层的好处：**职责单一、好测试、好替换**（比如把 ChromaDB 换成别的向量库，只需改 `retrieval_service.py`）。

---

## 4. 目录结构详解

```
20260810201631/
├── DESIGN.md                 # 技术设计方案（v1.1 评审修订版，900 行，值得全文读）
├── README.md                 # 项目说明 + 快速开始
├── .gitignore
├── backend/                  # FastAPI 后端
│   ├── main.py               # 入口：注册路由、CORS、鉴权中间件、异常处理、/health
│   ├── config.py             # 配置加载（YAML + .env + ${ENV_VAR} 展开 + 点分取值）
│   ├── config.yaml           # 配置文件
│   ├── database.py           # SQLite 连接 + 建表（sqlite3 标准库）
│   ├── requirements.txt      # Python 依赖
│   ├── models/               # Pydantic 数据模型
│   │   ├── common.py         # 统一响应 ok()/fail()
│   │   ├── knowledge_base.py
│   │   ├── document.py
│   │   └── chat.py
│   ├── routers/              # 路由层（API 端点）
│   │   ├── knowledge_base.py # /api/knowledge-bases
│   │   ├── document.py       # 文档上传/删除/重处理
│   │   ├── chat.py           # 问答（普通+流式）、会话、历史消息
│   │   └── system.py         # /health
│   ├── services/             # 业务逻辑层
│   │   ├── kb_service.py     # 知识库 CRUD
│   │   ├── document_service.py # 文档处理流水线
│   │   ├── embedding_service.py # Embedding（本地/OpenAI）
│   │   ├── retrieval_service.py # ChromaDB 封装
│   │   ├── llm_service.py    # LLM 调用（DeepSeek/OpenAI/Ollama，含 key 校验）
│   │   └── chat_service.py   # RAG 编排（检索→Prompt→生成→持久化）
│   ├── middlewares/
│   │   └── auth.py           # 可选 API Key 鉴权（纯 ASGI 中间件）
│   ├── utils/
│   │   ├── file_utils.py     # 魔数校验 + 文件名清洗
│   │   └── time_utils.py     # 统一 UTC 时间戳
│   ├── verify_retrieval.py   # 检索链路独立验证脚本（开发用，不依赖 LLM key）
│   ├── data/                 # 运行时数据（chroma/uploads/kb.db，gitignore）
│   └── tests/                # 测试目录（README 说明"预留"）
└── frontend/                 # React 前端
    ├── vite.config.ts        # Vite 配置 + /api 代理到 8000
    ├── package.json
    ├── index.html
    ├── tsconfig.json
    └── src/
        ├── main.tsx          # 入口（挂载 React，AntD 中文 locale）
        ├── App.tsx           # 路由表
        ├── types/index.ts    # TS 类型定义
        ├── api/              # API 调用层
        │   ├── client.ts     # axios 实例 + 响应拦截器（统一解包）
        │   ├── stream.ts     # SSE 流式客户端
        │   ├── knowledgeBase.ts / document.ts / chat.ts
        ├── components/       # 通用组件
        │   ├── Layout/ DocumentUpload/ MessageBubble/ ChatInput/ SessionSidebar/
        └── pages/            # 页面
            ├── KnowledgeBaseList/ KnowledgeBaseDetail/ ChatPage/
```

---

## 5. 核心业务流程（数据流）

### 5.1 文档上传与处理流水线

```
前端选择文件（FormData 上传）
        ↓
后端 router 读取文件内容 bytes
        ↓
① 大小校验（> 50MB 返回 413）
        ↓
② 类型校验：扩展名白名单 + 魔数校验（file_utils.detect_type）
        ↓
③ 文件名清洗（sanitize_filename），落盘为 {uuid}_{安全名}
        ↓
④ 创建 Document 记录，status = 'processing'
        ↓
⑤ BackgroundTasks 后台执行 process_document：
     解析文本(_extract_text) → 分块(_split_text) → 向量化(embed) → 入 ChromaDB
        ↓
⑥ 成功：status='completed' + chunk_count=N；失败：status='failed' + error_message
```

**关键点**：上传接口**立即返回** Document（此时还是 `processing`），真正的解析/向量化在**后台线程**跑。前端靠**轮询**（每 2 秒刷新）来看到状态变化。

### 5.2 RAG 问答流程（非流式）

```
用户提问 question
        ↓
① 确定会话：有合法 session_id 则复用，否则新建会话（title=问题前30字）
② 存用户消息（role='user'）
③ 问题向量化：embed_one(question)
④ ChromaDB 语义检索：top_k 个最相似片段
⑤ 计算相似度 = 1 - distance，过滤 < similarity_threshold(0.4) 的结果
⑥ 若过滤后为 0 条 → 直接返回"知识库中暂无相关信息"，【不调用 LLM】
⑦ 构建 Prompt（系统提示 + 参考资料 + 用户问题）
⑧ 调用 LLM 生成回答
⑨ 存助手消息（role='assistant'，含 sources 引用来源）
⑩ 返回完整 ChatMessage
```

### 5.3 RAG 问答流程（流式 SSE）

与非流式 ①~⑧ 相同，区别在于生成环节：

```
调用 llm_service.chat_stream() → 逐 token 产生
        ↓ 每收到一个 token：
          后端累积（collected.append）
          同时向前端推送 {"type":"token","content":"..."}
        ↓ 全部 token 结束：
          后端把完整回答 + sources 写入 SQLite（assistant 消息）
          推送 {"type":"sources", ...}
          推送 {"type":"done", "message_id":..., "session_id":...}
        ↓ 中途异常：
          推送 {"type":"error","content":"..."} 并终止
```

**重要约定**：流式问答中 **assistant 消息由后端持久化**，前端只负责逐字渲染，不负责存库。这样历史记录才完整、会话切换才有数据。

---

## 6. 后端逐文件解析

### 6.1 `main.py` —— 应用入口

- **`lifespan`**（`@asynccontextmanager`）：应用启动时执行 `init_db()` 建表，替代了旧版 FastAPI 的 `@app.on_event("startup")`。
- **CORS 中间件**：允许 `http://localhost:5173`（前端开发服务器）跨域。
- **`ApiKeyMiddleware`**：可选鉴权，放在 CORS 之后。
- **三个异常处理器**：
  - `HTTPException` → 按异常里的 status_code 返回统一格式；
  - `RequestValidationError` → 422；
  - `Exception`（兜底）→ 500。
- 最后 `include_router` 挂载四个路由。

### 6.2 `config.py` —— 配置加载

- 用 `yaml.safe_load` 读 `config.yaml`。
- **`_load_dotenv`**：先从 `backend/.env` 文件加载环境变量（支持 `KEY=value`、`KEY="value"`、`#` 注释，**不覆盖已存在的环境变量**）。这样密钥可写进 gitignore 的 `.env`，不用每次手动 export。
- **`_expand_env`**：递归把字符串里的 `${ENV_VAR}` 替换成环境变量值（用正则 `\$\{([A-Za-z_][A-Za-z0-9_]*)\}`），找不到就保留原文。这样密钥可以不写进 YAML。
- **`Config.get("a.b.c", default)`**：按 `.` 分割路径逐层取值，取不到返回默认值。
- **`resolve_path`**：把相对路径解析成基于 `backend/` 目录的绝对路径（因为运行时的工作目录可能不是 backend）。
- 模块加载时**先 `_load_dotenv()` 再 `load_config()`**，最后创建**全局单例** `config`。

### 6.3 `database.py` —— SQLite

- **`DDL`** 字符串：4 张表 + 3 个索引，全部 `CREATE TABLE IF NOT EXISTS`。
- **`get_connection`**：
  - `sqlite3.connect(...)`；
  - `conn.row_factory = sqlite3.Row`（让查询结果能**按列名**访问，像字典）；
  - `PRAGMA foreign_keys = ON`（**SQLite 默认不开启外键约束**，必须手动开，否则 `ON DELETE CASCADE` 不生效）。
- **`db()`** 上下文管理器：`yield conn` 正常则 `commit`，异常则 `rollback`，最后 `close`。这是事务的标准写法。
- **`init_db`**：用 `executescript` 执行整段 DDL。

### 6.4 `models/` —— Pydantic 模型

- `common.py`：`ok(data)` → `{"code":0,"data":...,"message":"success"}`；`fail(msg, code)` → `{"code":code,"data":None,"message":msg}`。
- `knowledge_base.py`：`KnowledgeBaseCreate`（`name` 用 `Field(min_length=1, max_length=200)` 做校验）、`KnowledgeBase`（含动态计算的 `document_count`）。
- `document.py`、`chat.py`：对应文档/会话/消息的 schema。

### 6.5 `services/` —— 业务逻辑（重点）

#### `kb_service.py`
- `create_kb`：生成 `uuid4()` 作为 id，`now_iso()` 时间戳，INSERT 后返回完整记录。
- `list_kbs` / `get_kb`：用**子查询**动态算 `document_count`：
  ```sql
  SELECT kb.*, (SELECT COUNT(*) FROM documents d WHERE d.kb_id = kb.id) AS document_count
  FROM knowledge_bases kb
  ```
- `delete_kb`：**顺序很重要**——先删 SQLite 记录（子表靠外键级联删），再删上传文件目录（`shutil.rmtree`），最后删 ChromaDB Collection。

#### `document_service.py`
- `_extract_text`：txt/md 直接 `Path.read_text`；pdf 用 `PyPDFLoader`；docx 用 `Docx2txtLoader`（都在函数内**惰性导入**）。
- `_split_text`：`RecursiveCharacterTextSplitter(chunk_size=500, chunk_overlap=50, separators=["\n\n","\n","。","！","？","；","，"," ",""])`——**中文标点作为分隔符**是关键，默认英文分隔符对中文切分质量差。
- `process_document`：核心流水线，全程 try/except，成功置 `completed`，失败置 `failed` + 错误信息。
- `delete_document`：先删向量，再删记录，再删文件。
- `start_reprocess`：**先删旧向量**（`delete_document_vectors`）再置 `processing`，避免重复向量。

#### `embedding_service.py`
- 模块级 `_local_model = None` 做**单例缓存**，避免每次调用都重新加载模型（模型加载很慢）。
- `embed(texts)`：`local` 模式用 `SentenceTransformer.encode(..., normalize_embeddings=True)`（归一化，配合 cosine）；`openai` 模式调 `client.embeddings.create`。

#### `retrieval_service.py`
- `get_client`：惰性导入 chromadb + 单例缓存 `PersistentClient`。
- `_collection`：`get_or_create_collection(name=f"kb_{kb_id}", metadata={"hnsw:space":"cosine"})`——**显式指定 cosine 度量**。
- `add_chunks`：**显式传 `embeddings=`**（不传的话 Chroma 会用它的默认模型联网下载，破坏本地一致性）。
- `search`：核心转换——**cosine 空间下 Chroma 返回的 `distances` 是"余弦距离"（= 1 − 相似度），所以 `similarity = 1 - distance`**。

#### `llm_service.py`
- `_llm_params`：根据 mode 返回 `(base_url, api_key, model, temperature, max_tokens)`。默认 `openai` 模式指向 **DeepSeek**（`https://api.deepseek.com/v1`，`deepseek-chat`）。Ollama 模式下 `base_url = "http://localhost:11434".rstrip("/") + "/v1"`——因为 **Ollama 提供了 OpenAI 兼容的 `/v1` 端点**，所以可以复用同一个 `OpenAI` SDK。
- **key 校验（新增）**：`openai` 模式下若 `api_key` 为空、或仍含 `${`（说明环境变量未设置/未展开），直接抛 `ValueError` 明确提示设置 `OPENAI_API_KEY`，而不是等到调用 LLM 时才报晦涩的认证错误。
- `chat`：一次性返回完整回答。
- `chat_stream`：`stream=True` 后逐 chunk 取 `delta.content` 并 `yield`（生成器）。

#### `chat_service.py`（RAG 编排核心）
- `SYSTEM_PROMPT`：提示词模板，`{chunks}` 是参考资料、`{question}` 是用户问题。
- `_ensure_session`：校验 `session_id` 是否属于该 kb，否则新建。
- `_save_message`：INSERT 消息 + 顺带 `UPDATE chat_sessions SET updated_at`（实现 §6.2 里说的"显式刷新 updated_at"）。
- `_retrieve`：向量化问题 → 检索 → 按阈值过滤。
- `_build_prompt`：把检索到的 chunk 编号拼接进模板。
- `chat`（非流式）与 `chat_stream`（流式）：完整编排整个 RAG 流程。
- `chat_stream` 外层 try/except 把任何异常统一转成 `error` 事件，**避免流中断导致前端一直挂起**。

### 6.6 `routers/` —— 端点

- 全部用**同步 `def`**（FastAPI 自动丢线程池）。
- `document.py` 的 `upload_document`：`file.file.read()` 一次性读入内存（个人场景文件小，可接受）→ 大小/类型校验 → `create_document_from_upload` → `background.add_task(process_document, ...)`。
- `chat.py` 的流式端点返回 `EventSourceResponse(chat_service.chat_stream(...))`。

### 6.7 `middlewares/auth.py` —— 鉴权

- **纯 ASGI 中间件**（自己实现 `__call__(scope, receive, send)`），而不是 `BaseHTTPMiddleware`。原因注释里写了：`BaseHTTPMiddleware` 会缓冲响应体，**破坏 SSE 流式响应**。
- `security.api_key` 为空时自动禁用；启用后校验 `/api/*` 的 `X-API-Key` 头；`OPTIONS`（CORS 预检）跳过鉴权。

### 6.8 `utils/`

- `file_utils.py`：
  - `sanitize_filename`：`Path(filename).name` 去掉路径、正则保留中文/字母/数字/下划线/连字符/点，防**路径穿越**。
  - `detect_type`：扩展名白名单 + **魔数校验**（PDF 以 `%PDF` 开头、DOCX 以 `PK\x03\x04` 开头）。txt/md 无可靠魔数，跳过。
- `time_utils.py`：`now_iso()` 返回**带时区的 UTC ISO 8601 时间戳**（微秒精度），统一入库格式，避免各机器本地时区混乱。

---

## 7. 前端逐文件解析

### 7.1 `main.tsx` / `App.tsx`

- `main.tsx`：`createRoot` 挂载 + `<ConfigProvider locale={zhCN}>`（AntD 中文化）+ `<React.StrictMode>`。
- `App.tsx`：路由表——
  - `/` → 重定向到 `/knowledge-bases`；
  - `/knowledge-bases` → 列表页；
  - `/knowledge-bases/:id` → 详情页；
  - `/knowledge-bases/:id/chat` → 对话页。
  - 外层 `<Route element={<Layout />}>` 用 `<Outlet />` 渲染子路由（嵌套路由）。

### 7.2 `api/client.ts` —— axios 封装

- `axios.create({ baseURL: '/api', timeout: 60000 })`。
- **响应拦截器**统一解包：后端返回 `{code, data, message}`，拦截器把 `data` 提取出来直接返回；`code !== 0` 时 `reject(new Error(message))`。
- 错误拦截器统一取出 `error.response.data.message`。
- 导出类型化的 `http.get/post/delete`。

### 7.3 `api/stream.ts` —— SSE 客户端

- 用 `@microsoft/fetch-event-source`（因为 **axios 不支持 SSE 增量读取**）。
- `fetchEventSource` 发起 POST，`onmessage` 里 `JSON.parse(ev.data)`，根据 `msg.type` 分发到 `onToken/onSources/onDone/onError`。
- `onerror` 里 `throw err` 来**停止默认重试**。
- 返回一个 `abort` 函数（`AbortController.abort()`）用于取消请求。

### 7.4 `pages/ChatPage/index.tsx` —— 最复杂的页面（流式渲染）

- **状态**：`sessions`、`messages`、`activeSessionId`、`streaming`、`streamingText`、`streamingSources`。
- **useRef 双缓冲**：`streamingTextRef` / `streamingSourcesRef` 累积 token 与来源。为什么用 ref？因为 `onToken` 回调里要基于**上一次的值累加**，而闭包里拿到的 state 可能是旧的（闭包陷阱），ref 的 `.current` 永远是实时值。
- **乐观渲染**：发送时先把用户消息"假"加进列表（id 用 `local-${Date.now()}`），提升响应感。
- `handleSend` 里 `streamChat(...)` 的 `onDone` 回调：把累积的 assistant 消息正式加入列表、更新会话 id、刷新会话列表。
- **清理**：`useEffect(() => () => abortRef.current?.(), [])` 组件卸载时中止流。

### 7.5 `pages/KnowledgeBaseDetail/index.tsx`

- 用 `Promise.all` 并发请求三个接口（知识库详情、文档列表、会话列表）。
- **轮询**：`hasProcessing = docs.some(d => d.status === 'processing')`，只要有文档在"处理中"，就 `setInterval(load, 2000)` 每 2 秒刷新，处理完自动停（`clearInterval`）。

### 7.6 其他组件

- `DocumentUpload`：AntD `Upload.Dragger` + `customRequest`（自定义上传逻辑，走自己的 `uploadDocument`），上传成功后调 `onUploaded` 刷新。
- `MessageBubble`：用户消息靠右蓝底，AI 靠左白底；AI 消息带 `Collapse` 折叠面板展示"引用来源"（文档名 + 相似度 + 原文，`ellipsis` 3 行可展开）。
- `ChatInput`：`Input.TextArea`，Enter 发送、Shift+Enter 换行（`onPressEnter` 判断 `e.shiftKey`）。
- `SessionSidebar`：会话列表 + "新对话"按钮。

---

## 8. 数据库设计

4 张表，外键关系：

```
KnowledgeBase 1 ── N Document
KnowledgeBase 1 ── N ChatSession
ChatSession   1 ── N ChatMessage
```

设计要点：

1. **`document_count` 不入库**：在查询时用子查询 `COUNT(*)` 动态算，避免和真实文档数不一致。
2. **`sources` 用 JSON 字符串存**：SQLite 没有 list 类型，存 `json.dumps` 的字符串，读出来 `json.loads`。
3. **`ON DELETE CASCADE`**：删知识库自动级联删文档/会话/消息（**前提是开了 `PRAGMA foreign_keys = ON`**）。
4. **`updated_at` 需手动刷新**：SQLite 的 `DEFAULT CURRENT_TIMESTAMP` 只在 INSERT 生效，UPDATE 不会自动更新，所以 `_save_message` 里显式 `UPDATE chat_sessions SET updated_at=?`。
5. **加了 3 个索引**（`documents.kb_id`、`chat_sessions.kb_id`、`chat_messages.session_id`），加速按外键查询。

---

## 9. 亮点设计（值得学习的地方）

1. **惰性导入 + 轻/重依赖分离**：torch、chromadb、langchain 都是"大而重"的依赖，全部在函数内部 `import`。没装这些时，`/health` 仍能跑，CRUD 也能用，只有问答/文档处理才报清晰错误。这让项目"先跑通骨架、再装重依赖"成为可能。
2. **单例缓存模型与客户端**：embedding 模型、Chroma client 都是模块级变量只初始化一次，避免重复加载开销。
3. **同步 `def` + 线程池的并发约定**：全项目统一，把阻塞调用丢给线程池，简单又正确。
4. **统一响应包裹**：`{code, data, message}`，前端拦截器统一解包，前后端协议清晰。
5. **安全细节**：魔数校验、文件名清洗防路径穿越、参数化 SQL 防注入、默认 `127.0.0.1`、可选 API Key、密钥走 `.env` 文件/环境变量。
6. **空检索兜底**：检索不到相关片段时直接返回"暂无相关信息"，不浪费 LLM 调用、也避免 LLM 硬编。
7. **流式异常兜底**：`chat_stream` 把任何异常转成 `error` 事件，避免前端一直"转圈"。
8. **纯 ASGI 鉴权中间件**：避开 `BaseHTTPMiddleware` 破坏 SSE 的坑，这是很容易踩的细节。
9. **`.env` 密钥管理 + LLM key 前置校验**：`config.py` 支持从 `backend/.env` 加载密钥（不覆盖已有环境变量），`llm_service` 在调用前就校验 key 是否缺失，把"配置错误"提前到最清晰的位置暴露。
10. **检索链路独立验证脚本**：`verify_retrieval.py` 不依赖 LLM key，单独验证 embedding + Chroma 检索，便于在接入 LLM 前定位检索问题。

---

## 10. 潜在问题与改进点（Review 视角）

> 面试时如果能主动说出这些，会非常加分——说明你不仅会"读代码"，还会"审代码"。

1. **上传文件一次性读入内存**：`file.file.read()` 对超大文件不友好，且 50MB 限制是在读完之后才判断。更好做法是**边读边写（流式/分块）**，先看 `Content-Length` 或边写边计数。
2. **`chat_stream` 中空检索分支**：无检索结果时，前端会收到逐字 `token`（因为 `for ch in answer`），但没有调用 LLM；逻辑正确，但可读性一般，可统一处理。
3. **无鉴权默认裸奔**：默认 `127.0.0.1` 只防了远程，但本机其他进程仍可访问；团队/局域网场景需主动开 `api_key`。
4. **并发下的会话竞态**：多个请求同时带同一个新 `session_id` 或同时创建会话，理论上可能有竞态（SQLite 单写，实际影响小，但值得知道）。
5. **Chroma 检索精度**：纯向量检索对"精确术语/编号/代码"类问题效果差，`DESIGN.md` 已预留混合检索（BM25）+ 重排（Rerank）作为后续增强。
6. **没有真正的分页**：文档/会话/消息列表一次性全量返回，数据量大时需加分页（`DESIGN.md` 已说明本期不做）。
7. **测试覆盖仍薄弱**：`tests/` 目录仍为空；虽有 `verify_retrieval.py` 可手动抽检检索，但还缺自动化单元测试/接口测试。
8. **前端轮询而非 WebSocket**：文档处理状态用 2 秒轮询，简单但不够实时；可考虑 SSE/WebSocket 推送状态。
9. **`file_type` 由扩展名+魔数决定，但 `.md`/`.txt` 无法用魔数强校验**，只能信扩展名（可接受，因为不执行内容）。

---

## 11. 端到端时序总结（一张图看懂全流程）

```
[用户上传 PDF]
   │
   ├─ 前端 FormData → POST /api/knowledge-bases/{id}/documents
   ├─ 后端校验(大小/类型) → 落盘 → DB status=processing → 返回
   ├─ BackgroundTasks 后台：解析→分块→embed→ChromaDB → status=completed
   └─ 前端每 2s 轮询刷新状态

[用户提问]
   │
   ├─ POST /api/knowledge-bases/{id}/chat/stream (SSE)
   ├─ 后端：新建/复用会话 → 存 user 消息 → 问题向量化
   ├─ ChromaDB 检索 top_k → 相似度过滤 → (空则兜底)
   ├─ 组 Prompt → LLM 流式生成
   ├─ 逐 token 推给前端（后端同时累积）
   └─ 结束后端存 assistant 消息 → 推 sources → 推 done
       前端 onDone：把 assistant 消息渲染进列表 + 更新会话
```

---

> 下一篇请看《知识点详解.md》，把上面每个名词（RAG、Embedding、向量库、SSE、Pydantic…）逐个拆开讲。
